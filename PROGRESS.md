# Progress

**Next up:** Task 27 — Visual/tone polish *(optional)*

Status legend: ⬜ not started · 🔶 in progress · ✅ done

## Foundation
- ✅ Task 1 — Project scaffold — `claude/dimensional-exploration-app-uj20jy` @ `cc3e7d9`, merged to default
- ✅ Task 2 — Testing infrastructure — `claude/dimensional-exploration-app-uj20jy` @ `cc3e7d9`, merged to default
- ✅ Task 3 — Global state store — `claude/task-3-baerqo` @ `ca95a8c`, merged to default
- ✅ Task 4 — Validation math — `claude/task-4-yi7axu` @ `3dad0cd`, merged to default
- ✅ Task 5 — Scene shell + stage router — `claude/task-5-lveobi` @ `ab2d3aa`, merged to default
- ✅ Task 6 — Orbit interaction tuning — `claude/task-6-ssl7u1` @ `777aec2`, merged to default
- ✅ Task 7 — Visual richness & material progression — `claude/visual-richness-material-progression-3m613e` @ `97aa056`, merged to default

## Drag interaction *(new session)*
- ✅ Task 8 — Drag-plane math — `claude/drag-plane-math-zto97s` @ `8bad661`, merged to default
- ✅ Task 9 — Arrow visual component — `claude/arrow-visual-component-aie9i7` @ `ae6fb89`, merged to default
- ✅ Task 10 — Pointer/drag controller — `claude/pointer-drag-controller-fxi0e5` @ `a54bdb7`, merged to default
- ✅ Task 11 — Validation wiring for Stages 1 & 2 — `claude/validation-wiring-stages-1-2-b9nq6a` @ `601f192`, merged to default

## Stage 3 *(new session)*
- ✅ Task 12 — Stage 3 (cube) + decomposition feedback — `claude/stage-3-cube-decomposition-wtq674` @ `67498bd`, merged to default

## Stage 4 *(new session)*
- ✅ Task 13 — 4D math core — `claude/4d-math-core-slicing-projection-evo2w8` @ `e0772b4`, merged to default
- ✅ Task 14 — Reveal scene — `claude/4d-math-core-slicing-projection-evo2w8` @ `e34ec99` (fixes the "slice always renders as a box" bug found in a live playtest of `c7d1603`), merged to default

## Instrumentation *(new session)*
- ✅ Task 15 — Live instrumentation panel (Stages 1–3) — `claude/live-instrumentation-panel-oo00wa` @ `6182fc4`, merged to default
- ✅ **Unplanned design pivot** (playtest feedback, between Tasks 15 and 16) — locked camera orbit on Stages 1–2 — `claude/live-instrumentation-panel-oo00wa` @ `9c37a6d`, merged to default. See notes below; Stage 2's visual-flatness-vs-winnability tension was **left as-is (not further flattened) by Task 16** — see Task 16's notes below for why.

## Stages 1–3 polish *(new session each — see PLAN.md's planning-session note after Task 15 for why these were inserted, and its "Camera per stage"/"Coordinate-space grids"/"On-screen camera controls"/"Advance timing" locked-decision rows for what they lock in)*
- ✅ Task 16 — Coordinate-space grid backgrounds + positive-octant repositioning — `claude/task-16-fhy311` @ `20e96e0`, merged to default
- ✅ Task 17 — On-screen directional controls replace mouse-drag camera orbit — `claude/task-17-gjyteb` @ `75643e3`, merged to default
- ✅ Task 18 — Let the player linger on a pass before advancing (Stages 1–2) — `claude/task-18-26x60x` @ `b861e95`, merged to default

## Stage 4 instrumentation *(new session)*
- ✅ Task 19 — Stage 4 instrumentation (rotation, tracked vertex, live cross-section data) — `claude/task-19-8s8ukz` @ `62d9a83`, merged to default

## Stage 4 extras *(new session)*
- ✅ Task 20 — 3D slicing warm-up (into Stage 4) — `claude/task-20-rmpxli` @ `85ee923`, merged to default
- ✅ Task 21 — Chirality / mirror-flip demo — `claude/task-21-rqc074` @ `52effb4`, merged to default

## Regression & polish *(new session)*
- ✅ Task 22 — Closing beat + restart — `claude/task-22-w9vvzb` @ `5da0174`, merged to default

## Playtest follow-ups *(new session each — inserted after a direct user playtest of the Task 22 build; see PLAN.md's three new "post-Task-22" locked-decision rows and rationale paragraphs for what they lock in)*
- ✅ Task 23 — Camera resistance/spring-back everywhere, instead of a per-stage hard lock — `worktree-agent-abff3af7f7676df66` @ `7a13af2`, **pushed straight to `claude/dimensional-exploration-app-uj20jy` (the default branch) on `origin`** — see note below (this session's sandbox, unlike the previous one, did have push access)
- ✅ Task 24 — Instrumentation panel redesign: cursor-tracking, plain-language-first, richer per-stage data — `worktree-agent-a83accdf558901cdb` @ `6f726b3`, **pushed straight to `claude/dimensional-exploration-app-uj20jy` (the default branch) on `origin`** — clean fast-forward, `626c76f..6f726b3`, confirmed via `git ls-remote`
- ✅ Task 25 — Dimension-growth transitions between stages, and the "can't grow into the 4th, but here's its shadow" beat — `claude/task-25-growth-transitions` @ `5b3f767`, merged to `claude/dimensional-exploration-app-uj20jy` (the default branch) via a clean local fast-forward and **pushed straight to `origin`** (`c38b79b..bd87225`, confirmed via `git ls-remote`)

## Regression & polish *(new session)*
- ✅ Task 26 — Full-playthrough regression test — `worktree-agent-ac13b5eff8bff854c` @ `249c154`, **pushed straight to `claude/dimensional-exploration-app-uj20jy` (the default branch) on `origin`** — clean fast-forward, `9cc45e5..249c154`, confirmed via `git ls-remote`
- ⬜ Task 27 — Visual/tone polish *(optional)*
- ⬜ Task 28 — Deploy *(optional)*

## Notes / deviations from PLAN.md

_(Add anything here that changed from the original plan as you go — tuned parameters, fallback approaches taken, anything a future session should know that isn't in PLAN.md.)_

- **Design pivot (after Task 6, before Task 7):** after trying the Foundation build locally, decided the original "minimalist throughout" tone (PLAN.md's old Tone/Cube-rendering rows) was too flat in 3D — wanted real textures/lighting/shader work, with visual richness escalating by dimension rather than staying uniform: Stage 1 (line) unchanged, Stage 2 (plane) transitional ("a cross between the two"), Stage 3 (cube) on gets the full textured/shaded/multi-light treatment. This added a new **Task 7 — Visual richness & material progression** to PLAN.md's Foundation group, which pushed every task from the old Task 7 onward up by one number (old Task 7 "Drag-plane math" is now Task 8, and so on through old Task 17 "Deploy" now Task 18). Nothing from Tasks 1-6 was renumbered or redone — only PLAN.md/PROGRESS.md's forward-looking task numbers and cross-references shifted; all git history, commit messages, and branch names for Tasks 1-6 keep their original numbers as committed. Task 5's flat cube material (`MeshStandardMaterial`, `opacity: 0.45`) and Task 6's orbit-tuning work stand as before — Task 7 supersedes the material, not the orbit or state work.
- `npm create vite@latest .` refuses to scaffold into a non-empty directory (this repo has CLAUDE.md/PLAN.md/PROGRESS.md) even with piped input; scaffolded into a scratch dir and copied the generated files in instead.
- The current `create-vite` react-ts template defaults to oxlint instead of ESLint. Removed oxlint/`.oxlintrc.json` and set up ESLint 10 (flat config) + `typescript-eslint` + `eslint-plugin-react-hooks`/`eslint-plugin-react-refresh` + Prettier (with `eslint-config-prettier` to disable stylistic overlap), per PLAN.md's "ESLint/Prettier" requirement. Note: `eslint-plugin-react-hooks`'s flat-config export for `recommended-latest` lives under `reactHooks.configs.flat['recommended-latest']`, not `reactHooks.configs['recommended-latest']` (that one is eslintrc-style and throws under flat config).
- Test cube uses `EdgesGeometry`/`lineSegments` rather than `meshBasicMaterial wireframe` on a `boxGeometry` — the latter draws the triangulation diagonals across each face, which looks wrong for the "sparse wireframe" tone; `EdgesGeometry` gives the expected clean 12-edge cube.
- Removed the template's default assets (`src/assets/*`, `public/icons.svg`) and the "Get started" landing page markup — replaced with a minimal dark `<Canvas>` shell directly in `App.tsx`. `scene/Experience.tsx` (Canvas + CameraControls + stage router) is still Task 5's job; for now `src/{state,math,scene,ui}` are just empty folders (`.gitkeep`) per the architecture skeleton.
- Verified via Playwright (headless Chromium at `/opt/pw-browsers`, launched directly since the `playwright` npm package isn't installed as a project dependency yet — that arrives with Task 2's e2e setup): page loads with zero console errors, two screenshots ~1.2s apart show the cube has visibly rotated.
- **Task 2:** Vitest config lives inline in `vite.config.ts` (via `/// <reference types="vitest/config" />`) rather than a separate file; `test.exclude` adds `e2e/**` so Playwright's `test()` doesn't collide with Vitest's collector (they'd otherwise both try to claim files under `e2e/`).
- This sandbox's preinstalled Playwright browser cache only has the regular Chromium build, not the `chromium_headless_shell` Playwright's default project wants — `playwright.config.ts` pins `use.launchOptions.executablePath` to `/opt/pw-browsers/chromium` to avoid a browser download attempt. Worth rechecking if this app is ever built/tested outside this sandbox.
- `e2e/screenshots/` is gitignored (Playwright output, regenerated by the test).
- **Task 3:** `lastResult`'s shape (`AttemptResult`) isn't specified by PLAN.md, since `evaluateAttempt` doesn't exist until Task 4. Went with `{ success: boolean; axisContributions: Record<Axis, number> }` — a per-axis (x/y/z) fraction of the dragged vector's magnitude — since Task 12 needs x/y/z percentages for the cube-stage decomposition feedback and this covers that without baking in more of Task 4's math than necessary. Task 4 should treat this as the target shape for `evaluateAttempt`'s return value rather than inventing a separate one, but it's free to adjust the field if it doesn't fit once the actual projection math is written.
- **Task 3:** Skipped the "optional" debug buttons for manually cycling `stage` (PLAN.md's verify step calls them optional) — no UI exists yet to hang them off until Task 5 builds the scene shell/HUD; `useDimensionsStore.getState().setStage(...)` is callable from the console in the meantime if needed.
- **Task 3:** `attempts` is a per-stage counter that resets on both `setStage` and `advanceStage`, not a running total across the whole playthrough — matches Task 12's "N attempts on the cube stage" use case.
- **Task 4:** Moved `AttemptResult` from `state/store.ts` into `math/validation.ts` (the module that actually produces it) and had the store import it — the more natural dependency direction, and what Task 3's note said Task 4 was free to do.
- **Task 4:** `projectOntoComplement` doesn't do a general vector projection — occupied axes are always a subset of the standard basis, so it's just zeroing the occupied components. Worth remembering if a future task ever needs projection onto a non-axis-aligned subspace; this function isn't that.
- **Task 4:** Used `ORTHOGONALITY_THRESHOLD = 0.7` exactly as specified in PLAN.md's tunables table. Test coverage straddles it with a 3-4-5 triangle (ratios 0.8 pass / 0.6 fail) rather than constructing a vector at the exact bit boundary, since an irrational hand-picked boundary vector risks flipping on floating-point rounding — the threshold constant itself is asserted directly instead.
- **Task 4:** `axisContributions` is computed from the raw drag vector (`|axis| / sum of |axis|`), independent of which axes are occupied — it's the same breakdown regardless of stage, so Stage 3's decomposition feedback and any other stage would see consistent numbers for the same drag.
- **Task 5:** `scene/stages/{Reveal}Stage.tsx` doesn't exist yet — the router's `switch` falls through to `null` for `'reveal'`/`'closing'`, so cycling to them via the debug buttons shows an empty scene (HUD text still updates correctly) rather than erroring. Tasks 14/15 add the real content; no router change needed then beyond adding the two cases.
- **Task 5:** Cube face material needed eye-tuning beyond PLAN.md's starting guess — first pass (`opacity: 0.28`, `color: #3a3a52`) read as barely-there wireframe-with-a-tint in the screenshot; bumped to `opacity: 0.45`, `color: #5858a0` (a lighter indigo) which clearly reads as solid while the far edges stay visible through the near faces. Superseded by Task 7's shader material — see the "Design pivot" note below. Worth another look once Task 10 has a mid-drag arrow inside the cube to check it stays legible too.
- **Task 5:** Camera framing per stage lives in `scene/cameraFraming.ts` as fixed position/target pairs, applied via `CameraControls.setLookAt(..., false)` (`false` = no transition) on stage change — straightforward instant cut for now; Task 11 is where an animated transition would replace the `false`.
- **Task 5:** Added `ui/DebugStageControls.tsx` (throwaway per Task 3's note) so Playwright — and anyone running `npm run dev` — can cycle stages without the real drag interaction that doesn't land until Tasks 8-11. It renders one button per `STAGE_ORDER` entry with `data-testid="debug-stage-{stage}"`. Delete this component once Task 11's real stage-advance wiring makes it redundant — don't let it survive into the shipped app.
- **Task 5:** Verified via Playwright (`e2e/stage-shell.spec.ts`): loads on Stage 1's line, clicks through all five stages, asserts the HUD label updates each time, and screenshots each — reviewed by hand (not just "test passed"), which is what caught the cube material issue above.
- **Task 6:** No config change was actually needed for "full 360° orbit, no clamping" — camera-controls' own defaults already give unrestricted azimuth (`±Infinity`) and the full polar range (`0`..`Math.PI`), plus comfortable damping (`smoothTime`/`draggingSmoothTime`). Made the orbit range explicit on `<CameraControls>` anyway (with a comment explaining why) so it's a stated decision instead of an implicit dependency on upstream defaults that could silently regress.
- **Task 6:** "Over the top" doesn't mean continuous wraparound past the zenith — camera-controls hard-clamps its internal polar angle to `[minPolarAngle, maxPolarAngle]` (0..π is the natural full range for spherical coordinates; going further would require an azimuth-flip trick the library doesn't do). So the Playwright test's bar is: dragging far past the pole settles cleanly at the extreme without erroring or getting stuck, not that the view spins past it to the other side.
- **Task 6:** `e2e/orbit.spec.ts`'s canvas-content assertions use `canvas.screenshot()`, not `canvas.toDataURL()` — the latter read back stale/blank pixels in a first pass, because three.js's `WebGLRenderer` runs with `preserveDrawingBuffer: false` by default. `page.screenshot()`/`locator.screenshot()` (compositor-based, what Task 5's test already used) is the reliable way to assert on WebGL canvas content in this app; worth remembering for any later e2e test that needs to detect a canvas change.
- **Task 6:** Confirmed Task 5's per-stage camera distances hold up off the default angle too — orbited the cube stage to a couple of arbitrary angles and it stayed comfortably framed both times, so no distance retuning was needed.
- **Task 7:** Went with a fully hand-rolled `THREE.ShaderMaterial` (per PLAN.md's recommended "procedural, noise/fresnel-driven `ShaderMaterial`" option) rather than injecting into `MeshStandardMaterial` via `onBeforeCompile` — simpler to reason about and get right without needing to match this three.js version's internal PBR shader chunk names. Because a custom `ShaderMaterial` doesn't read real scene lights at all, "multi-light setup" is delivered as two hand-coded light uniforms (key + fill) in the shader itself, `LIGHT_RIG` in `scene/materials.ts` is the single source of truth for their positions/colors/intensities, and `Experience.tsx`'s real `<directionalLight>`s (a second one now added, gated to stage `cube`/`reveal`/`closing`) mirror those same values — so the scene graph and the shader math stay numerically consistent even though the shader can't literally sample the `<directionalLight>` components.
- **Task 7:** Animates `uTime` via each material's own `onBeforeRender` callback (using `THREE.Timer`, not the deprecated `THREE.Clock`) rather than a `useFrame` callback mutating the memoized material — `eslint-plugin-react-hooks`'s v7 compiler-based checks (`recommended-latest`) flag `material.uniforms.uTime.value += delta` as "modifying a value returned from a hook" when done inside a second hook's callback (`useFrame`) after `useMemo` produced the material. Setting `onBeforeRender` during the material's own construction (inside the same `useMemo` factory) sidesteps the check entirely, since the mutation then happens inside a plain three.js callback, not across a hook boundary. Worth knowing for any future task that wants to mutate a memoized three.js object per-frame — `onBeforeRender`/`onBeforeCompile`-style escape hatches avoid this whole class of lint error that `useFrame` doesn't.
- **Task 7:** First pass at the cube material (`uOpacity: 0.5`, fresnel adding up to `+0.25` more) looked essentially opaque in screenshots — `DoubleSide` + `depthWrite: false` means several overlapping translucent layers (near face, far face, sometimes an open-corner interior face) alpha-blend on top of each other, and per-layer opacity compounds fast into a solid-looking block. Retuned to `uOpacity: 0.32` with fresnel's alpha contribution cut to `+0.1` (its brightness contribution stays bolder) — confirmed by eye across a few orbit angles (`e2e/screenshots/orbit-cube-angle-2.png`, `orbit-cube-angle-3.png`) that the far face and interior edges stay visible through the near faces again. Worth re-checking once Task 10 has a real mid-drag arrow inside the cube, same as Task 5's note.
- **Task 7:** `fbm`'s octave count is 2, not the more typical 3 — a quick frame-rate check (`e2e/richness.spec.ts`'s "sustains a reasonable frame rate" test) showed the cube's shader roughly halving fps versus the unlit line stage in this sandbox's software-rendered headless Chromium (~55-60fps line vs ~25-30fps cube at 3 octaves); dropping to 2 octaves recovered most of that without a visible loss of texture detail at the noise strengths used here. The test's floor is set to a generous `>15fps` rather than a tighter number, since this sandbox's absolute fps numbers are noisy and about the environment as much as the shader — the point is catching a pathological regression, not chasing a precise budget.
- **Task 7:** Plane stage's fill (`usePlaneFillMaterial`) uses a single light + weaker noise strength (no fresnel at all) versus the cube's two lights + fresnel — deliberately simpler, per PLAN.md leaving the exact "cross between" look undecided and asking for eye judgment. Landed on `uOpacity: 0.24` after an initial `0.12` pass read as almost invisible in the screenshot; the wireframe outline (rendered as a separate `lineSegments`, unchanged from Task 5) still reads as clearly primary against the fill at this level.
- **Task 7:** `e2e/richness.spec.ts` only asserts on `console.error`-type messages, not `console.warn` — `@react-three/fiber` itself logs a `THREE.Clock` deprecation warning on every mount (unrelated to this task's code, which uses `THREE.Timer`), and asserting on warnings would make the test depend on fixing an upstream dependency's internals.
- **Task 8:** `buildCameraFacingPlane`'s normal is the camera's forward direction (`camera.getWorldDirection()`), not the anchor-to-camera vector — the standard "billboard plane" approach (same idea `DragControls`/`TransformControls` use). These coincide whenever the camera is actually looking at the anchor, which is the case Task 10 needs (drag starts on the object currently in view), but the forward-direction version also works if that's ever not exactly true, so no reason to special-case it.
- **Task 8:** Both functions take the `Raycaster` (and `raycastPointerOntoPlane` also implicitly drives it via `setFromCamera`) as a parameter rather than constructing one internally — lets Task 10 reuse a single `Raycaster` instance across pointer-move events instead of allocating one per frame, and keeps the module DOM-free/pure per PLAN.md's ask. `pointerNDC` is a plain `Vector2` in normalized device coordinates (`[-1, 1]` each axis); computing that from a raw pointer event's page coordinates against the canvas rect is Task 10's job, not this module's.
- **Task 8:** Verified with hand-constructed `THREE.OrthographicCamera`s (not perspective) — an orthographic frustum makes the pointer-NDC-to-world-offset relationship exactly linear (`ndc * halfExtent`) with no perspective divide to compute by hand, which is what let the test comments hand-derive exact expected coordinates as PLAN.md's verify step asks for, including a case where the camera looks down a non-`z` world axis (confirms the plane/raycast genuinely follow the camera's orientation rather than assuming a fixed forward axis).
- **Session-start reconciliation (before Task 9):** `git remote show origin` reported the default branch (`claude/dimensional-exploration-app-uj20jy`) sitting at the pre-Task-7 "Replan" commit, with Task 7's and Task 8's work seemingly stranded on their own branches — looked exactly like the unmerged-work scenario CLAUDE.md's step 1 warns about. Turned out to be a stale local tracking ref (`origin/claude/dimensional-exploration-app-uj20jy` hadn't been freshly fetched this session) rather than an actual gap: `git fetch origin claude/dimensional-exploration-app-uj20jy` showed the real remote tip already at Task 8's commit (`9e34445`), and `PROGRESS.md` read from that fresh fetch already had Tasks 7-8 checked off correctly. No reconciliation was actually needed, but worth recording the pattern — always `git fetch` a specific branch before trusting a cached `origin/*` ref's SHA, `git remote show origin`'s cached view can lie the other way too (looking stale when it's actually current).
- **Task 9:** `useArrowMaterial(stage)` rebuilds the `ShaderMaterial` from scratch whenever `stage` changes (`useMemo` keyed on `[stage]`) rather than mutating `uniforms.uRichness`/`uLight2Intensity` on the existing material in a `useEffect` — the latter hit the same `eslint-plugin-react-hooks` compiler immutability check Task 7's note above describes for `onBeforeRender`, except this time it fired even from inside `useEffect` (not just a sibling hook's callback): "modifying a value returned from a hook is not allowed" the moment the mutation happens anywhere outside the `useMemo` factory that produced it. Rebuilding per stage change is cheap enough here (one arrow, five possible stages) that it wasn't worth fighting the lint rule further.
- **Task 9:** The arrow's accent color (`#ffb454` base / `#fff3d6` rim, warm gold) is deliberately a different hue family from every shape's cool indigo/violet palette (`#5858a0`, `#7d7cc4`, `#b6a6ff`) so it reads as "your input" rather than blending into the stage geometry — confirmed by eye in the cube-stage screenshots, where the gold arrow stays clearly legible both against the near translucent face and through to the far side.
- **Task 9:** Verified via a throwaway Playwright script (not committed — used the same preset buttons `DebugArrowControls` renders) clicking through all four presets (`hidden`/`short +x`/`tall +y`/`diagonal`) on each of the line/plane/cube stages and screenshotting; confirmed zero console/page errors, correct start-to-end pointing (including the cone consistently at the `end`-ward tip), live updates when switching presets, and legibility against Task 7's plane fill and cube shader. One debug preset (`diagonal`, originally `(-1,-1,-1)` to `(1,1,1)`) turned out nearly collinear with the default per-stage camera's view direction (`cameraFraming.ts`), so it foreshortened to a barely-visible blob in screenshots from the default angle — not a component bug, just an unlucky debug-vector choice; changed it to `(-1,-1,1)`→`(1,1,-1)` which reads clearly instead. Worth remembering if Task 10's real e2e drag test ever needs a diagonal-looking simulated drag: pick an end point oblique to `CAMERA_FRAMING`, not the literal shape diagonal.
- **Task 9:** `scene/LiveArrow.tsx` reuses one unit-sized (`radius 1, height 1`) `CylinderGeometry`/`ConeGeometry` pair at module scope across all arrow instances, scaling/positioning/rotating the mesh per `start`/`end` instead of rebuilding `BufferGeometry`s on every prop change — done with Task 10 in mind, since that task will update `start`/`end` on every pointer-move during a live drag, and rebuilding geometry that often would be wasteful compared to just updating a transform.
- **Task 10:** Confirmed `LiveArrow`'s plain `start`/`end` `Vector3` props were already exactly the right interface to drive from real pointer events — no changes needed there. Deleted `Experience.tsx`'s throwaway `DebugArrowControls`/`DEBUG_ARROWS` (Task 9's debug harness) now that `ArrowDrag` provides real input; the per-drag `start`/`end` points live in `ArrowDrag`'s own local `useState`, not the Zustand store, per CLAUDE.md/PLAN.md's Task 3/10 note — only the existing `isDrawing` boolean (already in the store since Task 3) is shared globally.
- **Task 10:** Each stage gets its own invisible, generously-sized collider mesh (`CylinderGeometry` for the line since it's a bare `lineSegments` with no raycastable surface at all; inflated `PlaneGeometry`/`BoxGeometry` for plane/cube) rather than raycasting the visible geometry directly — kept self-contained inside `ArrowDrag.tsx`, no changes needed to `LineStage`/`PlaneStage`/`CubeStage`. Colliders use `meshBasicMaterial transparent opacity={0}` rather than `visible={false}`, since three.js/`@react-three/fiber` raycasting behavior around invisible objects is inconsistent enough not to rely on — a fully transparent material is guaranteed to still raycast.
- **Task 10:** Once a drag starts (pointer-down hits a collider), pointer-move/up are handled via `window`-level listeners rather than the collider mesh's own R3F `onPointerMove`/`onPointerUp` — the whole point of the game is dragging *off* the shape, so a raycast-gated handler on the small collider mesh would stop firing the moment the pointer left it. `dragPlane.ts`'s `buildCameraFacingPlane`/`raycastPointerOntoPlane` were specifically designed for this (Task 8's note): the plane is built once at pointer-down from the real R3F hit point (`event.point`) and reused for the whole drag, then every `window` `pointermove` converts client coordinates to NDC by hand and raycasts onto that fixed plane.
- **Task 10:** "Discard drags too short to count" (PLAN.md) is implemented as a dead zone (`MIN_DRAG_LENGTH = 0.2` world units) rather than a check at pointer-up — below it, `LiveArrow` never renders at all, rather than rendering-then-discarding. There's no pass/fail logic yet for a *finished* drag to be discarded from (that's Task 11), so a live dead zone was the only way to give "too short to count" an actual observable effect in this task; Task 11 can replace/extend this with its own length or `evaluateAttempt`-based gating at pointer-up if a different threshold semantic turns out to be wanted.
- **Task 10:** Disabling/restoring `CameraControls` during a drag: mutating `.enabled` on the `useThree((state) => state.controls)` value directly trips `eslint-plugin-react-hooks`'s compiler-based immutability check (the same class of error Task 7/9 hit for `ShaderMaterial` mutation) — "modifying a value returned from a hook is not allowed." Fixed by mirroring that value into a locally-owned `useRef` (synced via a `useEffect`) and mutating `.enabled` through the ref instead, matching `Experience.tsx`'s `CameraRig`, which already calls methods on its own `useRef`-held `CameraControlsImpl` without issue.
- **Task 10:** `e2e/orbit.spec.ts`'s `dragCanvas` helper used to start every drag at canvas center — which, as of this task, is "on the object" for every stage (each stage's shape sits at the origin and the camera looks straight at it, per `cameraFraming.ts`), so those drags would now draw an arrow instead of orbiting. Changed the helper's start point to an 8%-inset corner, which the object's collider never reaches, so the existing orbit tests still test orbit; verified all three (plus the new cube-framing one) still pass.
- **Task 10:** Verified via `e2e/arrow-drag.spec.ts` (committed): an on-object drag on the line stage shows a mid-drag arrow then returns the canvas to pixel-exact parity with its pre-drag state on release (proving `CameraControls` stayed disabled and never moved); an off-object drag changes the final frame instead (orbit engaged, nothing drawn); a cube-stage test drags, orbits, and drags again, screenshotting both angles for a by-eye legibility check against Task 7's shader material. Also ran a throwaway (uncommitted) manual script exercising the plane stage the same way, and did a `npm run dev` look — all three stages behave correctly, zero console errors, and the mid-drag cube arrow reads clearly from two different orbit angles (screenshots reviewed by hand, echoing Task 5/7's note about checking this specifically once a real arrow existed).
- **Task 11:** On pointer-up, a drag clearing `MIN_DRAG_LENGTH` (Task 10's dead zone) is now handed to `evaluateAttempt(dragVector, STAGE_CONFIG[stage].occupiedAxes)`; success calls `advanceStage()`, failure shows a fail cue. Camera transition: `Experience.tsx`'s `CameraRig` now calls `setLookAt(...position, ...target, true)` — the `true` (Task 5 had left this `false`, an instant cut) enables camera-controls' own smoothed transition, no custom tweening needed.
- **Task 11:** The fail cue (`scene/FailCueArrow.tsx`) is a frozen copy of the finished drag that flashes red and fades over `FAIL_CUE_DURATION = 0.45s`, using its own plain `MeshBasicMaterial` rather than `LiveArrow`'s stage-driven `useArrowMaterial` shader — direct opacity control was easier to reason about than adding a fail-mode branch to the shared shader, and a cue that's about to disappear doesn't need per-stage richness. Animates opacity via `onBeforeRender` set during the material's own `useMemo` construction (same pattern as `materials.ts`'s `withTimeUniform`), not `useFrame` mutating the memoized material from outside — hits the identical `eslint-plugin-react-hooks` compiler immutability error Tasks 7/9/10's notes describe, and the same fix applies. `ArrowDrag` owns the timing via its own `setTimeout` (mirroring `FAIL_CUE_DURATION`) rather than the cue component reporting its own completion, since a `setTimeout` was simpler than adding a completion callback through another hook boundary.
- **Task 11:** `LiveArrow`'s shared geometries and shaft/head transform math moved out of `LiveArrow.tsx` into a new `scene/arrowGeometry.ts` so `FailCueArrow` could reuse them — not just a style choice: exporting non-component values from `LiveArrow.tsx` broke `eslint-plugin-react-hooks`'s `react-refresh/only-export-components` rule (a component file may only export components). `LiveArrow.tsx` now imports from `arrowGeometry.ts` and exports nothing else.
- **Task 11:** Left `ui/DebugStageControls.tsx` in place despite Task 5's note that Task 11's real stage-advance wiring should make it redundant and it should be deleted. In practice it's still load-bearing for jumping directly to Stage 2/3 in tests and manual exploration without replaying earlier stages every time (`e2e/arrow-drag.spec.ts`, `e2e/orbit.spec.ts`, `e2e/stage-shell.spec.ts`, and the new `e2e/validation-wiring.spec.ts` all use it this way) — removing it would mean reworking several existing tests to reach later stages via real drags, which is beyond this task's "validation wiring" scope. Worth revisiting in Task 23/24 (regression/polish, renumbered three times since — see the planning-session notes below) once there's no more need to jump stages directly for testing.
- **Task 11:** The plane stage's occupied-axes check (`['x', 'y']`, needing a drag mostly along `z`) turned out to have a much thinner pass margin than expected given `CAMERA_FRAMING.plane`'s camera position (`[3.5, 3, 4.5]` looking at the origin): swept drag angles empirically (no image-texture/analytic shortcut available since this uses the real perspective camera, not Task 8's hand-derived orthographic test cameras) and found the *best-case* orthogonality ratio achievable from any straight 2D screen drag tops out around ~0.715 — only marginally above `ORTHOGONALITY_THRESHOLD = 0.7`. Stage 1 (line) has a comfortable ~0.97 best case by contrast. Not fixed here (would mean retuning `CAMERA_FRAMING.plane`, a Task 5 concern, and risks a "while I'm here" scope violation) — flagging for a later polish pass if Stage 2 ever plays as too strict/fiddly in practice. The chosen `PLANE_PASS_DELTA` in `e2e/validation-wiring.spec.ts` (`{dx: 217, dy: -125}`) sits right at that best case.
- **Task 11:** `e2e/arrow-drag.spec.ts`'s first test (Task 10's) needed updating: its drag delta (`150, -100`) and its 300ms post-release wait both predate pass/fail logic existing at all. Changed the delta to one confirmed (empirically) to fail validation, so the test deterministically exercises drag mechanics rather than sometimes tripping a stage advance, and extended the post-release wait to 700ms so the new fail cue (450ms) has time to fully fade before the pixel-exact "back to at-rest" assertion. The new `e2e/validation-wiring.spec.ts` is where pass/fail outcomes themselves get tested.
- **Task 11:** Verified via `e2e/validation-wiring.spec.ts` (4 new tests: Stage 1 pass-with-animated-transition, Stage 1 fail-with-cue, Stage 2 pass-to-cube, Stage 2 fail-with-cue) plus the full existing suite (14/14 passing) and `npm test`/`npm run lint`/`npm run build`. Also ran a full by-hand playthrough via a throwaway (uncommitted) Playwright script driving `npm run dev` — Stage 1 fail → Stage 1 pass → Stage 2 fail → Stage 2 pass → two Stage 3 fails, zero console/page errors throughout, all screenshots reviewed by eye (fail cue reads clearly as a red flash on both wireframe and shaded stages, camera transitions land at the right per-stage framing, cube-stage fail cue stays legible against Task 7's shader material).
- **Task 12:** `ArrowDrag.tsx` needed zero changes — Task 11 already called `evaluateAttempt`/`recordAttempt` unconditionally for every stage including cube (occupiedAxes=['x','y','z'], so `evaluateAttempt` is structurally always a fail there per `validation.ts`'s own doc comment), so `attempts`/`lastResult.axisContributions` were already populated correctly on every cube drag before this task started. All that was actually missing was the *feedback UI* and the *stage-advance-without-a-pass* logic, both new: `ui/CubeFeedback.tsx` (reads `stage`/`attempts`/`lastResult` from the store) renders x/y/z decomposition percentages plus an "I understand" button once at least one attempt exists (`lastResult !== null` already implies attempts >= 1, so no separate "show after first attempt" flag was needed), and a `useEffect` in the same component calls `advanceStage()` once `attempts >= CUBE_ATTEMPTS_BEFORE_REVEAL` (new tunable in `state/stageConfig.ts`, defaulted to 3 per PLAN.md). Confirms the "no artificial lock" locked decision: `ArrowDrag`'s collider/drag handling is completely unmodified, so the player can keep attempting right up until the stage actually changes underneath them.
- **Task 12:** The auto-advance-to-reveal effect delays its `advanceStage()` call by `FAIL_CUE_DURATION` (0.45s, imported from `FailCueArrow.tsx`) rather than firing the instant `attempts` crosses the threshold — without the delay, hitting the Nth attempt would cut off that attempt's own fail cue mid-flash and yank the camera away; the delay lets it finish fading first, echoing the existing rhythm from Stages 1-2's pass/fail cues.
- **Task 12:** Verified via `e2e/cube-decomposition.spec.ts` (3 new tests: a cube drag stays on Stage 3 and shows plausible non-zero x/y/z percentages summing to ~100, three attempts auto-transitions to `reveal`, the "I understand" button transitions to `reveal` immediately after just one attempt) plus the full existing suite (17/17 passing) and `npm test`/`npm run lint`/`npm run build`. Also did a `npm run dev` look — screenshot (`e2e/screenshots/cube-decomposition-feedback.png`) reviewed by eye: percentages and the "I understand" button both read clearly against Task 7's cube shader material and don't collide with `DebugStageControls`' row at the very bottom.
- **Session note:** `npm install` had not been run yet at the start of this session (fresh clone/container) — `npm run lint` failed with `ERR_MODULE_NOT_FOUND` for `@eslint/js` until `npm install` was run first. Not a code issue, just a reminder that a fresh session needs `npm install` before any of the npm scripts will work.
- **Task 13:** Both PLAN.md-recommended techniques converged in one pass — neither fallback (hypersphere-slice, orthographic drop-w) was needed. No rendering was touched this session, per PLAN.md's "get both fully green before touching rendering" instruction; Task 14 is the first consumer of `math/fourd.ts`.
- **Task 13:** Faces are generated via the axis-pair method exactly as PLAN.md specifies (no hardcoded table): for each of the 6 free-axis pairs, walk the pair in Gray-code corner order `(-1,-1)→(1,-1)→(1,1)→(-1,1)` at each of the 4 sign combinations of the other 2 (fixed) axes, and resolve each corner's full 4D coordinates to a `TESSERACT_VERTICES` index via its ±1 bit pattern rather than an array search. `TESSERACT_EDGES` (32 edges) is generated independently the same way validation.ts's style favors small pure derivations — vertex-index pairs whose bit patterns differ in exactly one bit — rather than deriving edges from the face list; Task 14 will want it directly for the projection view's wireframe (faces alone don't give a flat edge list without walking each face's 4-cycle and deduping shared edges).
- **Task 13:** `sliceTesseract` slices each face's 4 edges directly (per PLAN.md), not a general polygon-hyperplane intersection — a face crossed by `w=w0` always has exactly 2 of its 4 edges cross (convexity), so `edgeCrossing` returning 0 or 1 point per edge and collecting exactly-2-per-face into one output segment is sufficient; no convex-hull step needed. `aw === bw` (an edge that doesn't move in `w`) short-circuits to "no crossing" rather than computing `0/0` — those edges belong to faces where `w` is a *fixed* axis, and any face actually intersected by the hyperplane has its 2 crossings supplied by its other, `w`-varying edges instead, so this never drops a real crossing. Only exercised the well-behaved case — the fully-degenerate one (`w0` exactly at `±1`, so an entire face lies flat inside the hyperplane) isn't covered by a test since the reveal-scene's slice offset isn't expected to sit exactly at that boundary; if Task 14 ever drives `w0` there, that face's edges all return "no crossing" (`aw===bw===w0` still short-circuits) so it silently contributes nothing rather than the small connecting square a true solid cross-section would have — worth a look only if Task 14 lets the player drag `w0` all the way to ±1.
- **Task 13:** `PROJECTION_VIEWER_DISTANCE = 3`, chosen only to safely clear the perspective divide's minimum safe denominator — rotation preserves `x²+w²` (=2 for a unit tesseract corner) so `|w|` never exceeds `√2 ≈ 1.414` for any rotation, and 3 leaves comfortable headroom (denominator ≥ ~1.59) without being so large that the perspective effect (PLAN.md's "classic rotating-tesseract" look) reads as flat. Not eye-tuned against a render yet since no rendering exists this session — flagged as a value Task 14 should sanity-check once the projection view is actually on screen, same spirit as Task 5/7's "re-tune by eye once you can see it" notes.
- **Task 14:** `PROJECTION_VIEWER_DISTANCE = 3` needed no retuning once actually on screen — the projection view reads exactly like the classic "cube inside a cube" tesseract shadow from the default reveal camera angle (`e2e/screenshots/reveal-projection-view.png`), confirming Task 13's headroom reasoning held up.
- **Task 14 (superseded by the post-merge fix below — kept for the record):** Originally only `rotateXW` was wired to player input, with `rotateYZ` deliberately left undriven on the reasoning that `yz` is an ordinary rotation `CameraControls`' free orbit already covers for free. That reasoning about `rotateYZ` still holds (it stays undriven), but it missed a consequence: rotating in *only* the xw plane confines the slice's cutting hyperplane to a normal with no y/z component, so the cross-section's y/z extent is always the unconstrained full `[-1, 1]` range — always a box, just skewed. A live playtest (the human, not this session) caught it immediately as the shape "just wobbling." See the fix note below.
- **Post-merge fix (same session, after user playtest):** Added `rotateYW` to `fourd.ts` and wired it to a Shift+drag in `RevealDrag.tsx` — regular drag still does `rotateXW` + slice offset; Shift+drag does `rotateYW` instead of `rotateXW`, still + slice offset. This is what actually breaks the "always a box" degeneracy, since the hyperplane's normal can now tilt into y. Renamed the store's `revealRotation` to `revealRotationXW`/`revealRotationYW` to track both planes independently (reset()/initialState/tests updated accordingly). Added an on-screen hint line in `RevealControls.tsx` ("drag to rotate · hold shift to rotate the other way · drag vertically to slice") since a keyboard modifier isn't self-discoverable otherwise. Confirmed by eye post-fix: a plain drag still just skews the box (expected — xw alone), but adding a Shift+drag on top produces genuinely different, more complex cross-section polytopes (screenshots taken during verification, not committed). Locked in with `fourd.test.ts` unit tests (the y-range-is-always-exactly-2 degeneracy under xw-only rotation, and that composing `rotateYW` breaks it — checked via *the range changing when `w0` changes*, not "the range shrinks," since composing `rotateYW` can make the range larger or smaller depending on the angles, not predictably smaller) and a new `e2e/reveal.spec.ts` regression test that drives the actual browser drag/shift-drag interaction and feeds the resulting rotation state through the app's own `fourd.ts` functions to confirm the fix holds end to end, not just in the isolated unit test. `zw` (the other axis that would also work) wasn't added — `yw` was arbitrary, same as `xw`'s original pick in Task 13.
- **Task 14:** The vertical drag component maps to `revealSliceW0` regardless of which view (`slice`/`projection`) is currently showing — dragging in projection view still moves the slice offset, it's just not visually expressed until you toggle back. This is what makes "one shared piece of 4D state, two views" actually true rather than nominal; verified by `e2e/reveal.spec.ts`'s toggle test (drags happen before toggling, and the post-toggle-back state is asserted `toEqual` the pre-toggle state, not just "close").
- **Task 14:** `REVEAL_SLICE_RANGE = 1.5` (stageConfig.ts) clamps `revealSliceW0` — comfortably past the true achievable `±√2` extent so the player can drag the cross-section all the way down to nothing (verified in a throwaway sweep during development: at a mild rotation, a hexahedral 12-edge slice holds steady, then collapses to 0 edges within one 0.1-unit step near the boundary — continuous shrink-then-vanish, not a discontinuity) without being able to drag indefinitely into empty space.
- **Task 14:** Canvas-pixel screenshot diffing (the technique `arrow-drag.spec.ts`/`validation-wiring.spec.ts` use for "did the scene change") turned out to be the wrong tool here and cost real debugging time before landing on the actual fix: `RevealStage`'s material is intentionally always animating (extends Task 7's noise system per PLAN.md rather than a flat `lineBasicMaterial`), and that ambient color drift alone produces a full-canvas byte-diff of the same order of magnitude as an actual drag-driven topology change (measured ~34k differing bytes for pure idle drift vs. ~19k-35k for various real drags, i.e. no reliable separation). Fixed by adding a dev-only `window.__dimensionsStore` hook in `store.ts` (guarded by `import.meta.env.DEV`, confirmed absent from the production `dist/` bundle via grep) so `e2e/reveal.spec.ts` asserts exact `revealRotationXW`/`revealRotationYW`/`revealSliceW0`/`revealView` values instead of inferring them from pixels — same "test-support scaffolding that doesn't ship" spirit as `DebugStageControls.tsx`, just for state instead of stage navigation. Worth reusing this hook (or extending it) for Task 15/16 if similar animated-material verification needs come up rather than re-fighting pixel diffing.
- **Task 14:** Verified via `e2e/reveal.spec.ts` (2 new tests: idle-vs-dragged state, and view-toggle-preserves-state) plus the full existing suite and `npm test`/`npm run lint`/`npm run build`. Two pre-existing failures reproduce identically against the Task 13 baseline commit (`git stash` + rerun) — `richness.spec.ts`'s frame-rate floor (13.9-15.0fps vs. >15 threshold, already documented as sandbox-noisy) and `validation-wiring.spec.ts`'s Stage 1 fail-cue test (passes/fails inconsistently across repeated runs, genuinely flaky, not a regression) — confirmed unrelated to this task and left alone rather than fixed, since neither is in scope for Task 14 and CLAUDE.md's task-boundary guidance says not to fold in "while I'm here" fixes. Worth a look during Task 23 (regression pass, renumbered three times since this note was written — see the planning-session notes below for each round) if they're still flaky then.
- **Task 14:** Also did a full `npm run dev` look with a throwaway (uncommitted) Playwright script: slice view starts as an exact cube (`w0=0` sanity check matching Task 13's known test case), dragging skews it into a non-cubic hexahedron rather than a dramatically different shape for modest drags (mathematically correct — most rotation/offset combinations still yield a 12-edge hexahedral cross-section; only near the `√2` boundary does the edge count actually drop, confirmed separately), and the projection view reads immediately and unmistakably as a tesseract shadow. Zero console/page errors throughout. One rough edge for a future polish pass (not fixed here, out of scope): at some rotation angles the projection view's shadow grows large enough to clip slightly against the canvas edge near the toggle button (`e2e/screenshots/reveal-projection-view.png` shows this) — `CAMERA_FRAMING.reveal` (Task 5's static per-stage framing) doesn't adapt to the projection view's variable extent; worth revisiting in Task 24 (visual/tone polish, renumbered three times since — see the planning-session notes below for each round) if it reads as a problem in practice rather than just tight framing.
- **Task 15:** `evaluateAttempt`'s `AttemptResult` gained a new required field, `orthogonalityRatio` (the same leftover/total ratio `success` is already thresholded against, just exposed numerically) — the panel calls `evaluateAttempt` directly against the live drag vector so the mid-drag number is exactly what pointer-up will use to decide pass/fail, rather than re-deriving the math. Additive/backward-compatible for every real caller (`ArrowDrag.tsx`, `CubeFeedback.tsx`), but the hand-built `AttemptResult` literals in `state/store.test.ts` needed the new field added too since it's a required property, not optional.
- **Task 15:** The one new store field (`liveDragVector: Vec3 | null`) went through two designs. First pass: populate it on every pointer-move but *don't* clear it on pointer-up, so the panel could keep showing the last attempt's numbers at rest (arguably a richer reading of PLAN.md's "the live drag value"). That broke two existing pixel-exact "canvas returns to at-rest" assertions (`e2e/arrow-drag.spec.ts`, `e2e/validation-wiring.spec.ts`'s Stage 1 fail-cue test) — turned out `page.locator('canvas').screenshot()` captures that screen *region* as rendered, including the DOM panel stacked visually on top of the WebGL canvas, not just the canvas's own paint layer, so a lingering non-placeholder value in the corner showed up as a real pixel diff. Fixed by mirroring `isDrawing`'s lifecycle exactly instead: `liveDragVector` is set on every pointer-move and cleared to `null` on pointer-up (same as `ArrowDrag.tsx`'s existing local `liveDrag`) as well as on stage change — which is also a more literal match for PLAN.md's own "the same kind of boundary-crossing `isDrawing` already does" wording. Worth remembering for Task 19 (Stage 4 instrumentation, renumbered twice since this note was written — see the planning-session notes below): any DOM readout addition needs to be checked against existing pixel-exact canvas-region screenshot assertions, not just against its own new tests.
- **Task 15:** `STAGE_SHAPE_COUNTS` (new export in `stageConfig.ts`) hardcodes Stages 1-3's vertex/edge counts (2v/1e, 4v/4e, 8v/12e) rather than deriving them from geometry at runtime — these are exactly the `n`-cube counts (`2^n` vertices, `n·2^(n-1)` edges) `math/fourd.ts`'s `TESSERACT_VERTICES`/`TESSERACT_EDGES` already instantiate for `n=4` (16v/32e), so Task 19 (Stage 4 instrumentation, renumbered twice since this note was written) can reuse that pattern directly rather than reinventing it. `reveal`/`closing` map to `null` in that table, which is what `DimensionPanel` uses to gate itself off entirely on those stages — Task 19 extends the panel rather than this session guessing at Stage 4's richer layout.
- **Task 15:** Verified via `e2e/instrumentation-panel.spec.ts` (3 new tests: Stage 1 shows no `x` row until a drag starts, then live `x`/orthogonality with `y`/`z`/`w` locked; Stage 2 lights up `x`+`y`; Stage 3 lights up `x`/`y`/`z` with orthogonality reading exactly `0.00`, confirming the cube's structural-failure property is live-computed, not a cached placeholder) plus the full existing suite (24/24 passing, including a rerun of the two tests the first design broke) and `npm test`/`npm run lint`/`npm run build`. Also did a `npm run dev` look with a throwaway (uncommitted) Playwright script, screenshotting mid-drag on all three stages and the idle/no-attempt state — reviewed by eye: the panel reads clearly in the top-right corner without colliding with `HUD`/`CubeFeedback`/`RevealControls`/`DebugStageControls`, and is correctly absent on the `reveal` stage.
- **Planning session (after Task 14, no code changed):** the user played the merged Task 14 build locally and, from that hands-on feedback, we worked through what would actually make the 4th dimension *fathomable* rather than just mathematically correct-but-abstract. That conversation added four new tasks to PLAN.md, inserted between Task 14 and the original Task 15 (renumbering the original Tasks 15-18 to 19-22 — same "insert before not-yet-started work" pattern as the Task 7 insertion documented above, so nothing built or merged so far needed touching): **Task 15** (live x/y/z/w instrumentation panel for Stages 1-3, with a three-way placeholder vocabulary — "Unknown, unreachable" for a structurally locked axis, "sliced away"/"hidden behind the shadow" for a real value the current view isn't showing, and no row at all for "nothing attempted yet" — since these are genuinely different kinds of absence and were flattening into one meaningless dash), **Task 16** (the same panel's Stage 4 extension: rotation in degrees, a single tracked tesseract vertex highlighted in the scene in the same color as its readout row so the number and the point read as one thing, live edge count, cross-section extent, and "distance from projection" framed as a real distance rather than an abstract scale multiplier so the closer-along-w-means-bigger-in-the-shadow logic is visible cause and effect), **Task 17** (a short 3D-solid-slicing warm-up before the tesseract slice, on the theory — confirmed by this session's own "why does this just wobble" confusion — that "moving a knife through a loaf of bread" needs to be established before "moving a hyperplane through a tesseract" can land), and **Task 18** (a chirality/mirror-flip demo: a simple asymmetric shape at `w=0`, flipped into its mirror image by the exact same `rotateXW`/`rotateYW`/`projectTo3D` drag control already built for the tesseract — verified during the discussion, not just asserted, that a 180° rotation in a w-mixing plane really is a mirror reflection once restricted to 3 visible axes, even though the full 4D transform stays a proper rotation throughout). See PLAN.md's "Live instrumentation," "Chirality demo" locked-decision rows and the four new rationale paragraphs after "Why Stage 4 shows the tesseract two ways" for the full reasoning — not duplicated here. Next session should read Task 15 fresh from PLAN.md rather than from this summary.
- **Design pivot (playtest feedback, after Task 15, before Task 16):** the user played the merged Task 15 build and flagged two problems. (1) "The first/second dimension actually exist in three dimensions — moving the screen rotates it in 3D, which defeats the point": `CameraControls`' free orbit was available on every stage, including Stages 1-2, so a player could drag the screen and watch "a line" or "a plane" turn out to be an ordinary object sitting in an orbit-able 3D scene, undermining the whole "this stage genuinely only has N axes" premise. (2) The drag mechanic doubles as the pass/fail test with no separate exploration phase, so a successful first attempt immediately advances the stage — "you don't get enough of a chance to play with it." **Only (1) was fixed this session** (matches the user's own "the very first thing we should sort out" priority); (2) is unaddressed and flagged for a future task — it needs its own design decision (e.g. a "confirm to continue" step mirroring `CubeFeedback`'s existing "I understand" pattern) rather than a guess.
- **Design pivot:** `cameraFraming.ts`'s `CameraFraming` gained `orbitEnabled: boolean`. Stage 1 (line) also moved to a straight-on, dead-center framing (`[0,0,5]` looking at the origin, versus the old oblique `[3.5,2,4]`) — with nothing else in the scene and orbit locked, it now reads unambiguously as "this is all there is." Confirmed still fully winnable: "leaving the line" only needs a y-or-z component, and a dead-on-z camera's drag plane still lets y vary freely (only z is pinned), so a vertical drag reads ~0.97 orthogonality, same as before.
- **Design pivot — the open tension (read before Task 16 or any further Stage 1-2 camera work):** Stage 2 (plane) deliberately did **not** get the same dead-on treatment — first attempt did, and it broke the stage outright. `dragPlane.ts`'s camera-facing drag plane uses the camera's own forward direction as its normal; when the camera looks exactly down -z (dead-on), that drag plane *is* the z=0 plane, so every possible 2D mouse drag is mathematically confined to x/y — z (the exact axis "leaving the plane" needs) becomes permanently unreachable, not just harder. This isn't a tuning problem, it's structural: the more visually "flat/2D" Stage 2's camera gets, the less possible it geometrically is to drag "out of the page" at all (and Task 11's own notes already flagged the *original* oblique angle's pass margin as tight, ~0.715 vs. 0.7 threshold — flattening further would likely push it under 0.7 entirely). Reverted Stage 2 to its original oblique framing (kept `orbitEnabled: false` — no orbit, but still a visibly-3D-looking static parallelogram) as the safe fix for this session. Real options for a future task, not decided here: (a) accept a compromise tilt partway between flat and the current angle, empirically retuning `PLANE_PASS_DELTA`/`PLANE_FAIL_DELTA` in `validation-wiring.spec.ts` and accepting an even tighter pass margin; (b) decouple "leaving the plane" from camera obliqueness entirely — e.g. a Shift+drag mapped explicitly to z, mirroring the exact modifier-key pattern `RevealDrag.tsx` already uses to pick `rotateXW` vs `rotateYW`; (c) something else. This needs the user's input, not a unilateral pick — flagging rather than guessing.
- **Design pivot:** Fixing Stage 1/2's orbit lock broke two existing tests that implicitly relied on the old "every stage orbits" behavior — `e2e/orbit.spec.ts`'s two orbit-mechanics tests and `e2e/arrow-drag.spec.ts`'s "off-object drag orbits" test all used to run (undeclared) on the default Stage 1; moved all three to explicitly target the cube stage, which is where orbit still lives. Added two new `orbit.spec.ts` tests confirming the lock itself (Stage 1 and Stage 2, dragging off-object does nothing) — Stage 2's needed a dev-only `window.__cameraControls` hook (`Experience.tsx`, same spirit as `store.ts`'s `window.__dimensionsStore`) reading `azimuthAngle`/`polarAngle`/`distance` directly, since Stage 2's fill material's own idle animation (Task 7's noise shader) makes a canvas-pixel comparison unable to distinguish "orbit did nothing" from "orbit did something too subtle to see" — the exact class of problem Task 14's notes already describe for the reveal stage. Also replaced a fixed `waitForTimeout` guess (for `CameraControls`' damped `setLookAt` transition to settle before reading exact camera state) with a poll-until-two-consecutive-reads-match helper (`waitForCameraSettled`) after a fixed 700ms wait turned out to still occasionally catch the transition's slow-converging tail mid-flight in this environment — worth reusing this polling pattern for any future test reading exact camera state right after a stage transition.
- **Design pivot — real bug found and fixed via this work, not the camera itself:** the dead-on Stage 1 framing initially seemed to break `validation-wiring.spec.ts`'s existing Stage 1 fail-cue test (a horizontal "fail" drag renders byte-identical to no-drag-at-all at the test's 100ms check). Deep-dived with temporary instrumentation (removed before commit) rather than assuming it was the camera's fault, and confirmed via `git stash` against the exact pre-session baseline that **this fails 3/5 times on unmodified Task 15 code too** — a pre-existing bug, already vaguely flagged as "genuinely flaky" in Task 14's notes, just newly reproducing at a much higher rate in this environment. Now root-caused precisely (worth recording since "flaky" undersells it): `FailCueArrow`'s fade uses `THREE.Timer`, which measures real wall-clock time via `performance.now()`, not frames-rendered — `material.opacity` is being computed correctly every single frame (confirmed via direct instrumentation: 0.64 → 0.71 → 0.55 → 0.35... a proper flicker/fade over ~450ms), but if the *browser's main thread stalls* between two animation frames for any reason (confirmed cause here: Playwright's own `page.screenshot()` call, which does real CPU work — compositing, PNG encoding, a CDP round-trip), `Timer.getElapsed()` reports the *full* real time that passed, including the stall, and the fade can appear to complete almost instantly on whatever frame renders next. Not a camera or Task 15 regression — pure pre-existing timing fragility in a wall-clock-driven fade fighting against test/screenshot overhead. Left unfixed (real fix means moving off `performance.now()`-based timing onto a frame/render-count-driven fade, or decoupling the *visual* fade from `ArrowDrag`'s `setTimeout`-driven unmount — out of scope for a camera-lock task); confirmed **not** a NEW regression from this session's changes, so not blocking. Worth a real fix in Task 23 (regression pass, renumbered twice since this note was written — see the planning-session notes below) rather than continuing to shrug at it as vague flakiness.
- **Design pivot:** Verified via the full `npm test`/`npm run lint`/`npm run build` plus the full Playwright suite (25/26 passing on the run used to sign off; the one failure is the pre-existing fail-cue timing bug documented above, confirmed unrelated). Also did a `npm run dev` look with a throwaway (uncommitted) Playwright script: Stage 1 now reads as a single flat line with zero depth cues and a fully inert off-object drag; Stage 2 still visibly reads as a tilted 3D parallelogram (the open tension above) but no longer orbits; Stage 3 (cube) orbit still works exactly as before. Zero console/page errors throughout.
- **Planning session (after the design-pivot camera fix, no code changed):** the user reviewed the camera-lock fix and raised a follow-on design idea (a per-stage coordinate-space grid background — dotted line / x-y graph paper / positive-octant-only 3D grid, so each stage's dimensionality is visually asserted, not just implied by the absence of orbit) and re-flagged the still-outstanding "let the player linger on a pass" item from the prior round. Both are deliberately **not** built this session — sized as their own tasks and inserted into PLAN.md between Task 15 and the original Task 16 (renumbering the original Tasks 16-22 to 18-24 — same "insert before not-yet-started work" pattern as the Task 7 insertion and the post-Task-14 four-task insertion documented above): **Task 16** (coordinate-space grids + repositioning every shape into the positive octant to match a grid that only covers positive coordinates — also the task responsible for actually deciding Stage 2's still-open camera tension flagged above, not just working around it) and **Task 17** (stop auto-advancing the instant a Stage 1-2 drag passes; show feedback and let the player keep exploring, mirroring `CubeFeedback`'s existing "I understand" pattern, until they choose to continue). Split into two separate sessions (not one) since they're different enough concerns — scene geometry/coordinate system vs. game-loop/interaction timing — matching this doc's usual granularity for two related-but-distinct pieces of work (same reasoning as the original Task 15/18 split). See PLAN.md's three new "Camera per stage"/"Coordinate-space grids"/"Advance timing" locked-decision rows, the two new "Why" rationale paragraphs after "Why the chirality demo," and the two new tunables entries for the full detail — not duplicated here. While renumbering, also fixed several older notes above (Task 11, 14, 15) whose task-number cross-references had already gone stale from this and the prior renumbering — worth double-checking historical notes for stale task numbers any time PLAN.md's task list shifts, since they're easy to miss (this session found ones that had survived uncorrected through at least one prior renumbering already). Next session should read Task 16 fresh from PLAN.md rather than from this summary.
- **Task 16:** Every Stages 1-3 shape's geometry stayed centered on its own local origin (unchanged) — a single per-stage translation vector (`scene/shapePositions.ts`'s `SHAPE_POSITION`) is applied as the mesh/group's world `position` instead, and that exact same vector is added to `cameraFraming.ts`'s old origin-centered `position`/`target` and to `ArrowDrag.tsx`'s collider `position`. Translating the shape, the camera, and the collider by one shared vector keeps the camera's relative angle/distance to the shape identical to before, so a mouse drag's NDC-delta-to-world-vector mapping is only shifted, never reshaped — this is what let every pre-Task-16 drag/validation/instrumentation test pass **unmodified** at the new coordinates (they all drag from canvas center, which still lands on the shape since the camera's `target` moved with it), exactly as PLAN.md's own note predicted. Final placements: line `0→3` along `x`, plane `0→3` in `x`/`y`, cube `0→2.5` in each axis with one corner pinned to the world origin.
- **Task 16:** `scene/Grid.tsx` builds each stage's grid as plain `BufferGeometry` line segments (a `LineDashedMaterial` two-point line for Stage 1's dotted number-line; `lineSegments` for Stage 2's graph paper and Stage 3's three room-corner planes) rather than reusing `THREE.GridHelper` — `GridHelper` is a symmetric square centered on its own origin with no positive-octant-only mode, and Task 16's whole point is a grid that visibly stops at an edge rather than extending in every direction. Each grid plane is offset a hair (`COPLANAR_EPSILON = 0.01`) behind its shape's coincident face, to avoid z-fighting between the grid's lines and the shape's own coplanar geometry (most visible on the cube, whose three near faces sit exactly on the three grid planes by design — "a corner of a room"). Extents/spacing (`LINE_GRID_EXTENT = 4.5`, `PLANE_GRID_EXTENT = 4.5`/`PLANE_GRID_SPACING = 0.75`, `CUBE_GRID_EXTENT = 4`/`CUBE_GRID_SPACING = 1`) were picked by eye per PLAN.md's tunable note — confirmed via screenshots that every shape sits fully inside its grid with a bit of margin, not flush against the edge.
- **Task 16 — real bug found and fixed via this work, not the repositioning itself:** repositioning Stage 1/2's camera `target` off world-origin broke `e2e/orbit.spec.ts`'s and `e2e/validation-wiring.spec.ts`'s "canvas returns to an exact pixel match" assertions intermittently on mount. Root cause: `Experience.tsx`'s `CameraRig` always called `setLookAt(..., true)` (an animated/smoothed transition) on every stage change, including the very first one on mount — this never mattered pre-Task-16 because every `CAMERA_FRAMING` target used to be `(0,0,0)`, which happens to be `CameraControls`' own implicit default target, so the "animated" transition on mount had nothing to actually travel and settled instantly. Once Stage 1/2's targets moved off-origin, that implicit default target no longer matched, so the very first `setLookAt` had a real (if small) distance to smoothly travel — confirmed by direct measurement (a throwaway Playwright script polling `window.__cameraControls`) that azimuth/distance were still visibly mid-settle (e.g. azimuth `0.17` rad drifting toward `0` over ~600ms) well past the fixed `waitForTimeout(300)` several existing tests use before taking their "before" screenshot. Fixed with a `hasMountedRef` in `CameraRig`: the very first `setLookAt` call (on mount) stays an instant cut (`false`), exactly like Task 5's original behavior before Task 11 made every transition animated; every subsequent real stage-advance transition still animates as before. Confirmed via `git stash` against the pre-Task-16 baseline that this exact race does **not** reproduce there (all relevant tests passed cleanly pre-fix), so this was a genuine Task-16-caused regression, not a pre-existing issue — unlike the flake noted below.
- **Task 16 — confirmed pre-existing flake, left unfixed:** `e2e/orbit.spec.ts`'s "Stage 2: the camera is locked — dragging off the plane does nothing" test calls its `waitForCameraSettled` helper immediately after clicking the Stage-2 debug button, with **no wait at all** before its first `window.__cameraControls` read — a genuine race against `CameraRig`'s mount-time `useEffect` actually running. Confirmed via `git stash` against the exact pre-Task-16 baseline, using the identical reproduction script, that `window.__cameraControls` reads `undefined` immediately after such a click on unmodified Task-15-pivot code too, and that the real Playwright test itself fails there at roughly the same rate pre- and post-Task-16 (measured: baseline 1/3 passes, this session's code 1/5 passes across separate small samples — noisy, but not a clean regression). This is the same class of issue Task 14's notes already flagged for the fail-cue timing bug ("flaky" undersells a race that's actually deterministic once you look at *why*) — not fixed here per CLAUDE.md's task-boundary guidance (the bug is in a shared test helper `orbit.spec.ts` owns, unrelated to grids/repositioning), flagging for Task 23 (regression pass) instead. The one-line fix, for whoever picks this up: give `waitForCameraSettled` an initial wait (or an existence-poll for `window.__cameraControls` itself) before its first read, matching how `dragCanvas`'s own helper already waits before reading camera state elsewhere in the same file.
- **Task 16 — mid-task planning pivot (Stage 2's camera tension):** PLAN.md's own tunables note for this task was explicit that Task 16 needed to *decide* Stage 2's still-open camera tension (dead-on vs. oblique), not just reposition around it, and that prior notes said this decision needed the user's input rather than a unilateral pick. Asked via `AskUserQuestion`, offering the three previously-flagged options (compromise tilt / dedicated Shift+drag-to-z / defer). The user's answer was a fourth option not on the list: replace mouse-drag camera orbit with on-screen directional controls everywhere, reserving mouse-drag exclusively for "pointing to the next dimension" (the existing axis-testing/rotation drag mechanic) — which resolves Stage 2's tension by removing its root cause (the same drag currently means "orbit" or "point" depending on incidental pointer position) rather than tuning the camera angle at all. Confirmed with the user that this is a much larger change than Task 16's own scope — it touches `ArrowDrag.tsx`, `RevealDrag.tsx`, `dragPlane.ts`, and the camera-orbit wiring across every stage, not just Stage 2's framing — and, per this codebase's own established convention for design pivots this size (see the "Planning session" notes above), recorded it as a new **Task 17 — On-screen directional controls replace mouse-drag camera orbit**, inserted into PLAN.md between Task 16 and the original Task 17 (renumbering the original Tasks 17-24 to 18-25 — same pattern as every prior insertion documented above). Task 16 itself proceeded and finished exactly as scoped (grids + positive-octant repositioning) — Stage 2's camera framing (`CAMERA_FRAMING.plane`) was **not** touched, deliberately, since retuning it now would be exactly the "tune around it" PLAN.md said not to do; it's left for Task 17 to actually resolve. See PLAN.md's new "On-screen camera controls" locked-decision row, the "Why on-screen camera controls (Task 17)" rationale paragraph, and Task 17's own deliverable/tunable text for the full detail — not duplicated here. Next session should read Task 17 fresh from PLAN.md rather than from this summary.
- **Task 16:** Verified via `npm test` (70/70), `npm run lint`, `npm run build`, and the full Playwright suite (26/26 passing on the run used to sign off, after the `CameraRig` mount-transition fix above; the one intermittent failure across repeated runs is the confirmed-pre-existing `orbit.spec.ts` Stage-2 flake documented above). Also did an `npm run dev` look with a throwaway (uncommitted) Playwright script, screenshotting all three stages (plus the cube stage orbited to a second angle): Stage 1's line sits at the start of its dotted number-line with room to spare; Stage 2's plane sits fully inside its graph-paper grid, still reading as a tilted 3D parallelogram (the still-open tension, now handed to Task 17); Stage 3's cube sits snugly in the corner where its three grid-planes meet, unmistakably reading as "standing in the corner of a room" from multiple orbit angles, with `CubeFeedback`'s x/y/z readout and "I understand" button still legible and non-colliding. Zero console/page errors throughout.

- **Task 17 — the two open UX questions PLAN.md left to this task's judgment:** (1) *whether Stages 1-2 gain any camera movement at all.* Asked the user directly via `AskUserQuestion` rather than guessing, since PLAN.md's own tunable note flagged this as needing "a real answer, not an assumption." The user picked the middle option: Stage 1 stays fully locked (no buttons, matches its existing "this is all there is" treatment); Stage 2 gets a *limited* button range rather than none or the unclamped free range Stage 3+ gets. (2) *whether Stage 2's resting framing should then change.* Once buttons exist to let the player deliberately tilt away from dead-on before dragging, the geometric constraint that forced Stage 2 to stay oblique (Task 16's notes: a dead-on camera's drag-plane normal *is* the z=0 plane, so z becomes undraggable) no longer has to hold at the *resting* angle — so Stage 2 moved to the same dead-on framing as Stage 1 (`position: [1.5,1.5,5]`, `target: [1.5,1.5,0]`), which is what actually makes it read as "a flat 2D world" the way Task 16's notes said the old oblique compromise never fully could.
- **Task 17:** `CameraControls.enabled` is now permanently `false` (a static prop on `<CameraControls>` in `Experience.tsx`, not toggled anywhere) — mouse-drag never orbits the camera on any stage, full stop, replacing the old per-stage `orbitEnabled` toggle that `ArrowDrag.tsx`/`RevealDrag.tsx` used to flip on pointer-down/up. Both of those files lost that toggle logic entirely (and their now-unused `useThree((state) => state.controls)` selectors) since there's nothing left to disable during a drag. `cameraFraming.ts`'s `CameraFraming` interface renamed `orbitEnabled` → `cameraControlsEnabled` (now gates whether `ui/CameraDirectionalControls.tsx`'s *buttons* render/work, not mouse input) and gained `azimuthRange`/`polarRange` (absolute radians in `camera-controls`' own fixed reference frame) so `Experience.tsx`'s `CameraRig` can pass per-stage `minAzimuthAngle`/`maxAzimuthAngle`/`minPolarAngle`/`maxPolarAngle` — Stage 3 on keeps the original unclamped full range, Stage 2 gets `[-PLANE_TILT_RANGE, PLANE_TILT_RANGE]`/`[pi/2-PLANE_TILT_RANGE, pi/2+PLANE_TILT_RANGE]`, Stage 1's is unused (buttons never render there).
- **Task 17:** `ui/CameraDirectionalControls.tsx` (bottom-right corner, clear of `DebugStageControls`/`CubeFeedback`/`RevealControls`) renders a 4-button rotate D-pad plus 2 zoom buttons, calling `.rotate()`/`.dolly()` on the shared `CameraControls` instance — it's a plain DOM overlay outside the R3F `Canvas` (same tree position as `HUD`/`DimensionPanel`/etc. in `App.tsx`), so it has no `useThree()` access to that instance. New module `scene/cameraControlsRef.ts` exports a plain mutable `{ current: CameraControlsImpl | null }` ref that `CameraRig` populates on mount and the button component reads on click — the production-code equivalent of the existing dev-only `window.__cameraControls` hook, needed here because button clicks aren't dev-only. Each button fires once on click and keeps firing on a `HOLD_REPEAT_MS = 90` interval while held down (`RepeatButton`), rather than requiring one click per `ROTATE_STEP`/`DOLLY_STEP` increment.
- **Task 17:** `PLANE_TILT_RANGE` (how far Stage 2's buttons may tilt from dead-on) was swept empirically rather than picked, the same spirit as Task 11's original oblique-angle deltas — no analytic shortcut exists once real perspective-camera geometry is involved (same reasoning Task 11's notes give). Method: drive real button clicks + real mouse drags in a running `npm run dev` instance via a throwaway Playwright script (not committed), reading `window.__dimensionsStore.getState().liveDragVector` mid-drag to compute the achievable orthogonality ratio directly, and separately confirming pass/fail against the real `evaluateAttempt` call at pointer-up (mid-drag ratio and actual pass/fail agreed once the sweep script was fixed — see below). Results: `0.6` rad only reaches a best-case ratio of ~0.565 (short of `ORTHOGONALITY_THRESHOLD = 0.7`); `0.8` clears it but only right at the margin (~0.71, and a real pointer-up test at that exact margin failed to advance the stage on one run despite a passing mid-drag reading — too close to the boundary to be reliable); `1.0` clears it comfortably (~0.83 best case, confirmed passing consistently across repeated resets). Landed on `1.0`. `PLANE_PASS_DELTA = {dx: 100, dy: 0}` / `PLANE_FAIL_DELTA = {dx: 0, dy: -200}` in `validation-wiring.spec.ts` are both now applied *after* 3 clicks of the "rotate right" button (`PLANE_TILT_CLICKS = 3`, `ROTATE_STEP = 0.35` rad/click clamps to exactly `PLANE_TILT_RANGE = 1.0`), replacing the old oblique-camera deltas that no longer apply now that Stage 2 rests dead-on.
- **Task 17 — sweep-script pitfalls worth recording for any future empirical tuning session:** two false leads cost real time before landing on the numbers above. (1) A `page.locator('canvas').boundingBox()` call made immediately after `page.goto()` with no wait returned a stale/undersized box from before React/Three.js had mounted and resized the canvas — every synthetic drag computed from it landed off-screen, producing spurious "the object was missed" results that looked like a geometry problem but were actually a scripting bug. Always wait (or wait for an explicit visible/loaded assertion) before computing `boundingBox()`, matching what every committed e2e test already does. (2) A sweep script that reused one page session across multiple drag deltas without checking `stage` between attempts silently kept testing on the *next* stage once an early delta happened to pass and `advanceStage()` fired — every subsequent "measurement" in that batch was then meaningless (missed collider, wrong camera). Any script sweeping deltas near a pass threshold needs to check (and reset) `stage` between attempts, not just assume it stayed put.
- **Task 17 — real pre-existing flake found and fixed via this work (not a new regression, but the rewritten tests depend on it directly):** `orbit.spec.ts`'s `waitForCameraSettled` helper — already flagged in Task 16's notes as calling `window.__cameraControls` immediately after a stage-changing click with no initial wait, a genuine race against `Experience.tsx`'s `CameraRig` mount-time `useEffect` — reproduced at a high enough rate (multiple failures across repeated full-suite runs, both in `orbit.spec.ts`'s own Stage-2 test and in a new `arrow-drag.spec.ts` test built on the same pattern) that leaving it as documented-but-unfixed would have shipped Task 17's own new tests flaky from day one. Applied the exact one-line fix Task 16's notes already prescribed: poll for `window.__cameraControls` to exist before the first read, in both `orbit.spec.ts` (`waitForCameraControls`) and the equivalent inline logic added to `arrow-drag.spec.ts`'s new off-object-drag test. Confirmed fixed via 6 repeated runs of each affected test post-fix (0 failures) versus 2-3/5 failing pre-fix.
- **Task 17:** Rewrote `orbit.spec.ts` end to end around button clicks (`clickRepeatedly` helper) instead of `dragCanvas`-style off-object drags, since off-object drag no longer orbits the camera on any stage — kept the same mechanics coverage (unclamped full-revolution rotation, reaching the polar pole without locking up, reframing across a few angles, per-stage lock/range checks) just driven differently. `arrow-drag.spec.ts`'s "drag starting off the object orbits the camera instead of drawing" test became "...does nothing" (camera-state equality, not a pixel diff, since the cube's shader animates on its own); its cube-legibility test's "orbit to a different angle via an off-object drag" step became "click the rotate-right button 5 times" — the shape stays camera-target-locked so canvas center is still on-object after rotating via buttons, unlike after a drag-orbit.
- **Task 17:** Verified via `npm test` (70/70), `npm run lint`, `npm run build`, and the full Playwright suite (26/26), run clean twice in a row plus targeted repeats (5-6x each) of every test this task touched or added, after the flake fix above. Also did an `npm run dev` look via a throwaway (uncommitted) Playwright script, screenshotting Stage 1 (no buttons, unchanged), Stage 2 at rest (now dead-on, reads unambiguously flat, buttons visible bottom-right), Stage 2 tilted via 3 button clicks (clearly reveals the plane's edge/depth, exactly the intended "deliberate action" feel), Stage 3 (buttons don't collide with `CubeFeedback`'s x/y/z readout or "I understand" button), and Stage 4/reveal (buttons don't collide with `RevealControls`' hint text or view-toggle button) — reviewed by eye, all read cleanly with zero console/page errors.

**Context-window check-in:** this session covered Task 17 end to end — reading PLAN.md/PROGRESS.md, one `AskUserQuestion` round on the open Stage 1-2 camera-movement question, the implementation itself, an empirical `PLANE_TILT_RANGE` sweep (several throwaway Playwright scripts, two rounds of debugging scripting mistakes before trusting the results), a full e2e test rewrite across three spec files, and fixing a pre-existing test-infra flake the rewritten tests exposed at a much higher rate. Conversation context was not auto-summarized during this session. This is a single-task session (per CLAUDE.md's "exactly one task" rule) — the next session should start fresh from Task 18.

- **Task 18:** New store field `stagePassed` (`state/store.ts`) plus one new action `markStagePassed()` — deliberately separate from `lastResult`/`recordAttempt`, since a later failed attempt on the same stage (the player continuing to explore after their first pass) must not un-pass it and hide the Continue button. `ArrowDrag.tsx`'s pointer-up success branch now calls `markStagePassed()` instead of `advanceStage()` when `stage === 'line' || stage === 'plane'` — cube (and any other stage) still advances immediately, matching the old behavior, though in practice `evaluateAttempt` never returns success on the cube stage at all (structural fail, per `stageConfig.ts`'s `CUBE_ATTEMPTS_BEFORE_REVEAL` doc comment). Reset alongside the other per-stage fields on `setStage`/`advanceStage`/`reset`.
- **Task 18:** New `ui/StageContinue.tsx` (bottom-center, same position/style `CubeFeedback.tsx` uses for the cube stage — no collision since the two never show at once) renders a message + "Continue" button when `stage` is `line`/`plane` and `stagePassed` is true; the button calls `advanceStage()` directly. Went with **manual button only, no fallback auto-advance** — PLAN.md's tunable note left the choice open ("following `CubeFeedback`'s precedent either way"), but unlike the cube stage (which needs a way to move the player past a state they can never pass out of), Stages 1-2's player has already succeeded — forcing a timeout here would undercut the whole point of "let them linger as long as they like."
- **Task 18:** Updated `e2e/validation-wiring.spec.ts`'s two "advances to next stage" tests (Stage 1→2, Stage 2→3): a pass no longer changes `stage` immediately, so both now assert the stage stays put and `stage-continue-button` appears, and only advance after clicking it. Extended the Stage 1 test to also cover the "keep exploring" behavior PLAN.md's verify step calls for directly: after the pass, a second (failing) drag on the same stage still shows the fail cue and leaves the Continue button visible/unaffected, confirming `stagePassed` genuinely survives a later fail rather than needing a fresh pass every time.
- **Task 18:** Verified via `npm test` (74/74, including 4 new `store.test.ts` cases for `markStagePassed`/`stagePassed`'s lifecycle), `npm run lint`, `npm run build`, and the full Playwright suite (25/26 — the one failure is the pre-existing, already-documented `richness.spec.ts` frame-rate-floor flake, sandbox-noise unrelated to this task, reproducing at 14.7fps vs. the >15 threshold). Also did an `npm run dev` look via a throwaway (uncommitted) Playwright script: Stage 1's pass message/button read cleanly against the line/dotted-grid background with zero collision against `DimensionPanel`/`DebugStageControls`, and clicking Continue transitions cleanly into Stage 2's dead-on framing with zero console/page errors throughout.

**Context-window check-in:** this session covered Task 18 end to end in one pass — reconciliation (confirmed no unmerged branch work), reading PLAN.md/PROGRESS.md, the store/component implementation, updating the two affected e2e tests plus new store unit tests, and a manual `npm run dev` visual check. No `AskUserQuestion` was needed (PLAN.md's one open tunable — manual button vs. button-plus-fallback — was resolved by the reasoning above, not a UX call needing the user). Conversation context was not auto-summarized during this session. Single-task session per CLAUDE.md — the next session should start fresh from Task 19.

- **Task 19:** Added `math/fourd.ts`'s `applyRevealRotation(vertices, rotationXW, rotationYW)` (just `rotateYW(rotateXW(...))`, the exact composition `RevealStage.tsx` already used) so `RevealStage.tsx`'s render and `DimensionPanel.tsx`'s new readouts share one source of truth for the rotation order instead of each re-deriving it — the panel independently recomputes the tracked vertex/edge-count/extent from the store's `revealRotationXW`/`revealRotationYW`/`revealSliceW0` via this helper plus `sliceTesseract`, the same "call the real function, don't thread a derived value through the store" approach Task 15's distance/orthogonality readouts already used.
- **Task 19:** Which vertex to track (PLAN.md left this arbitrary) — picked the all-positive corner `(1,1,1,1)`, found via `TESSERACT_VERTICES.findIndex(...)` rather than a hardcoded index, in a new small `scene/trackedVertex.ts` that also holds the shared `TRACKED_VERTEX_COLOR` (`#5eebff`, cyan — distinct from the shapes' indigo/violet and the arrow's gold) so `RevealStage.tsx`'s marker mesh and `DimensionPanel.tsx`'s ledger rows import the same two constants instead of duplicating either.
- **Task 19:** The tracked-vertex marker (a small `SphereGeometry` in `RevealStage.tsx`) uses each view's own coordinate treatment rather than one fixed transform: raw `x/y/z` in slice view (matching `sliceTesseract`'s crossing points, which are never perspective-scaled) and `projectTo3D`'s scaled position in projection view (matching every other projected vertex the wireframe draws). This means the marker lands exactly on one of the wireframe's own corners in projection view (confirmed by eye — `e2e/screenshots/instrumentation-stage4-projection.png`), and in slice view it floats through space at the vertex's true unsliced position, only landing on the visible cross-section when its rotated `w` happens to pass through `w0` — this is deliberate, not a bug: PLAN.md's "the connection between the number and the point is immediate" only requires the marker to track the same live point the ledger describes, not that it always sit on the currently-visible geometry.
- **Task 19 — resolving an ambiguity in PLAN.md's own wording:** the task's placeholder-vocabulary sentence ("edge count while in projection, distance-from-projection while in slice" reads "sliced away"/"hidden behind the shadow" respectively) doesn't parse consistently against the same paragraph's earlier, unambiguous statement that edge count is a real live number in *both* views (dynamic 0-12 in slice, fixed 32 in projection — "the contrast... is itself worth the player noticing"). Read literally, edge count can't be both "a real number always" and "a placeholder while in projection." Treated this as a wording slip rather than two genuinely conflicting requirements: edge count never shows a placeholder (always real, per the unambiguous sentence), and the two placeholder-eligible fields are the ones actually described as view-specific — cross-section extent (slice-only concept) reads `"hidden behind the shadow"` in projection view, distance-from-projection/shadow-scale (projection-only concept) reads `"sliced away"` in slice view. PLAN.md's own tunables section explicitly leaves "the exact phrasing" free to retune as long as the three absence-cases stay visibly distinct, which this satisfies; flagging the source ambiguity here in case a future session reads it differently.
- **Task 19:** Reused Task 15's `dimension-row-x`/`-y`/`-z`/`-w` test ids for the tracked vertex's ledger on the reveal stage (rather than inventing new ids) since PLAN.md frames this explicitly as "the same ledger from Task 15" — the panel now branches internally (`RevealDimensionPanel`, not exported, called from `DimensionPanel` when `stage === 'reveal'`) rather than being a second top-level component, so `App.tsx` didn't need to change.
- **Task 19:** Verified via `npm test` (77/77, including a new `fourd.test.ts` case confirming `applyRevealRotation` matches the hand-composed `rotateYW(rotateXW(...))` every existing caller used), `npm run lint`, `npm run build`, `npx prettier --check` (caught and fixed formatting on the two touched files), and the full Playwright suite (30/30 — 2 new tests added to `e2e/instrumentation-panel.spec.ts`: one confirming the rotation/w0/tracked-vertex/edge-count readouts match `math/fourd.ts`'s own functions fed the exact drag-driven store state via the `window.__dimensionsStore` hook, same technique `e2e/reveal.spec.ts`'s regression test already uses — not a re-derivation of the panel's own arithmetic; one confirming both placeholder phrases appear in the expected view and disappear in the other, with real extent/distance numbers otherwise). Also did an `npm run dev` look (a throwaway, uncommitted Playwright script) at the reveal stage's idle state (`w0`/rotation both `0` — the tracked vertex's ledger already shows real numbers immediately, unlike Stages 1-3's drag-gated panel, since there's no "attempt" concept in Stage 4) — zero console/page errors, panel reads legibly, no collision with `RevealControls`' hint text/toggle button or `CameraDirectionalControls`.

**Context-window check-in:** this session covered Task 19 end to end in one pass — reconciliation (confirmed all prior `claude/*` branches already merged into default), reading PLAN.md/PROGRESS.md, the math/scene/panel implementation, new unit and Playwright tests, a prettier formatting pass, and a manual `npm run dev` visual check of both the idle and dragged states. No `AskUserQuestion` was needed — PLAN.md's one open tunable (which vertex to track) was explicitly arbitrary, and the placeholder-wording ambiguity was resolved by reasoning through the paragraph's own internal consistency rather than needing a UX call. Conversation context was not auto-summarized during this session. Single-task session per CLAUDE.md — the next session should start fresh from Task 20, and per PLAN.md's own marker, Task 20 (slicing warm-up) and Task 21 (chirality demo) are both fresh, self-contained subsystems rather than extensions of the instrumentation panel.

- **Task 20:** New pure-math module `math/coneSlice.ts` approximates a cone as a triangulated solid (an apex, an N=28-sided rim, and a base cap — `N+2` vertices, `2N` triangular faces, `3N` wireframe edges, all generated rather than hardcoded) and reuses `fourd.ts`'s `sliceTesseract` *technique* — per-face edge-crossing, exactly-2-crossings-per-crossed-face — one dimension down: triangular faces (3 edges, 0-or-2 crossings) instead of the tesseract's quads (4 edges). Deliberately its own module with its own `Point3`/`Point2` types rather than importing `fourd.ts`'s `Point3`/`Vec4` — this is an ordinary-3D-space rehearsal, not a 4D concept, same "self-contained per feature" precedent `validation.ts`'s own `Vec3` type already sets.
- **Task 20:** The cone *tilts* (mixing y into z, `tiltCone`) against an always-fixed `z=z0` cutting plane, rather than the plane tilting against a fixed cone — this mirrors `fourd.ts`'s own approach exactly (the tesseract rotates; the hyperplane `w=w0` never does), and a single tilt axis is already enough to break the "always a circle" degeneracy (the one-dimension-down analogue of `rotateXW`-alone's "always a box" degeneracy) since the cutting plane's normal is fixed at `z` regardless of rotation — unlike the tesseract, no second rotation plane was needed. Locked in with a `coneSlice.test.ts` case mirroring `fourd.test.ts`'s own degeneracy-and-fix tests. A near-apex slice degenerates naturally to a cluster of near-zero-length segments at the apex point — PLAN.md's "a point" case falls out of the same edge-crossing math with no special-casing.
- **Task 20:** `scene/SliceWarmup.tsx` owns both the drag interaction *and* the render in one component/file, deliberately not split into a `RevealStage`/`RevealDrag`-style pair mediated by the global store: nothing outside this component needs the cone's tilt/slice-offset state (no instrumentation panel reads it, unlike `revealRotationXW`/`revealSliceW0`), so it's plain local `useState`, per CLAUDE.md/PLAN.md's Task 3/10 note that per-interaction state nothing else needs stays local — a new store slice would've been unjustified plumbing. The drag itself reuses `dragPlane.ts`'s camera-facing-plane raycast exactly as `RevealDrag.tsx` does (horizontal delta → tilt, vertical delta → slice offset, no Shift-modifier needed since only 2 values need controlling here vs. the tesseract's 3), starting from `RevealDrag.tsx`'s own `ROTATION_SENSITIVITY`/`SLICE_SENSITIVITY` constants as a tuning baseline since it plays out against the identical reveal-stage camera framing.
- **Task 20:** The resulting 2D cross-section is drawn "alongside" the cone as a second `lineSegments` group offset `+2.6` along world x (scaled up `1.6x` vs. the cone's own `1.3x`, for legibility against the smaller raw unit-radius coordinates), with a faint dark backdrop plane behind it so it reads as its own separate 2D view rather than floating lines in the same 3D space. The cone's wireframe reuses `useTesseractLineMaterial()` verbatim (the exact material the tesseract itself uses right after this hands off) rather than a new material — deliberately continues the same visual language into the real Stage 4 content; the cutting-plane visual and the cross-section lines use the arrow's existing warm-gold accent (`#ffb454`) instead, matching this codebase's established "cool = shape, warm = the player's controlled input" color convention.
- **Task 20 — the setStage/advanceStage asymmetry (read before touching `revealWarmupActive`):** `state/store.ts`'s new `revealWarmupActive` field is set `true` **only** by `advanceStage()` landing on `'reveal'` (real gameplay progression from the cube stage's "give up"/auto-advance) — `setStage(stage)` always sets it `false`, even when jumping straight to `'reveal'`. This is deliberate, not an oversight: `ui/DebugStageControls.tsx` and every pre-existing reveal-stage Playwright test (`reveal.spec.ts`, `instrumentation-panel.spec.ts`) use `setStage`'s `debug-stage-reveal` button to jump straight to the tesseract for testing convenience, and none of them needed updating because of it — confirmed by running the full pre-existing suite unmodified after this change (still 32/32, including all of `reveal.spec.ts`). `e2e/slice-warmup.spec.ts` has an explicit regression test locking this asymmetry in (`debug-stage-reveal still bypasses the warm-up, unlike real progression`) so a future session doesn't "fix" it into symmetry and silently break every existing reveal-stage test. Reaching the warm-up in a test means going through the real cube→give-up→reveal path instead (see that spec's `reachRevealViaRealProgression` helper).
- **Task 20:** `RevealDrag.tsx`'s own collider now also gates on `!revealWarmupActive` (so its always-mounted pointer-down handler doesn't compete with `SliceWarmup`'s own collider while both could theoretically be mounted), and `ui/RevealControls.tsx`/`ui/DimensionPanel.tsx`'s `RevealDimensionPanel` both hide during the warm-up — showing the tesseract's rotation/slice-offset/tracked-vertex readouts or its view-toggle button while the cone plays would show stale, meaningless numbers/controls for a shape that isn't on screen yet.
- **Task 20:** Added a dev-only `window.__sliceWarmupState` hook (`SliceWarmup.tsx`, guarded by `import.meta.env.DEV`), same spirit as `store.ts`'s `window.__dimensionsStore` and `Experience.tsx`'s `window.__cameraControls` — needed because `tilt`/`sliceZ0` are local state with no store presence, and the cone's wireframe material animates its own color independently of drag input (same `useTesseractLineMaterial` issue `reveal.spec.ts`'s notes already describe for the tesseract), so a canvas-pixel diff can't reliably distinguish "the drag changed the cone" from "the shader's color drifted." `e2e/slice-warmup.spec.ts` reads this hook directly instead, mirroring `reveal.spec.ts`'s own established technique exactly.
- **Task 20:** Verified via `npm test` (96/96, including 15 new `coneSlice.test.ts` cases and 6 new `store.test.ts` cases for `revealWarmupActive`/`finishRevealWarmup`), `npm run lint`, `npx tsc --noEmit`, `npx prettier --check` (on every touched/new file — pre-existing formatting warnings on files this task didn't touch were left alone, matching Task 19's precedent of only formatting what was actually touched), `npm run build`, and the full Playwright suite (32/32, 4 new tests in `e2e/slice-warmup.spec.ts`: warm-up shows instead of the tesseract on real progression, `debug-stage-reveal` still bypasses it, a drag changes both `tilt` and `sliceZ0` and the on-screen cross-section, and skipping mid-interaction hands off cleanly into the existing tesseract UI). Also did an `npm run dev` look via a throwaway (uncommitted) Playwright script — screenshots confirmed the cone reads clearly as a wireframe cone from the reveal camera's default angle, the cutting plane and its cross-section both track a drag (circle → ellipse → larger ellipse across a couple of drags), and skip transitions into the tesseract with `RevealControls`/`DimensionPanel` both correctly reappearing — zero console/page errors throughout. One thing intentionally left for a future polish pass rather than fixed here: the cross-section inset can grow large enough to approach the right edge of the canvas at some tilt/offset combinations (same class of "variable-extent content vs. fixed `CAMERA_FRAMING`" rough edge Task 14's notes already flagged for the projection view) — not a bug, just worth a look in Task 24 if it reads as cramped in practice.

**Context-window check-in:** this session covered Task 20 end to end in one pass — reading PLAN.md/PROGRESS.md, designing and building the cone-slicing math/scene/store/UI pieces, writing and running new unit + Playwright tests, a manual `npm run dev` visual check with screenshots sent to the user, and this PROGRESS.md update. No `AskUserQuestion` was needed — every open PLAN.md tunable for this task (which solid, exactly how long it plays, how obviously skippable) was explicitly left to this task's own judgment, not a user decision. Conversation context was not auto-summarized during this session. Single-task session per CLAUDE.md — the next session should start fresh from Task 21 (chirality/mirror-flip demo), which PLAN.md's own "new session recommended" marker (before Task 20) already called out as a fresh, self-contained subsystem independent of this one.

- **Task 21 — where the demo sits in the flow (PLAN.md's own open tunable for this task):** went with **a third `revealView` state** (`'slice' | 'projection' | 'chirality'`), cycled by the same `ui/RevealControls.tsx` toggle button, over the other two PLAN.md options (a separate timed/threshold beat, or its own small stage). This wasn't picked to avoid a decision — PLAN.md's own locked-decision row for the chirality demo already says it reuses "the exact same drag control already driving `revealRotationXW`/`revealRotationYW`" and "`rotateXW`/`rotateYW`/`projectTo3D` verbatim," which only makes sense if the demo reads the *same* store fields the tesseract does rather than a parallel copy — the third-view design is the one that actually matches that sentence; a separate stage or timed beat would need its own rotation state (or awkward state-sharing across a stage boundary) for no real benefit.
- **Task 21:** New pure module `math/chirality.ts` defines `CHIRALITY_PARTS` — a palm (box) + four graduated-length fingers + an off-center thumb (cylinders), each just a `{ shape, size, anchor }` with the anchor at `w=0` — deliberately asymmetric about every axis (checked directly in `chirality.test.ts`) so the mirror-flip has something real to flip; a symmetric arrangement would look identical before and after and defeat the demo's point. No rotation/projection math lives here at all, per the locked decision above — `scene/ChiralityDemo.tsx` is the only caller, and it imports `rotateXW`/`applyRevealRotation`/`projectTo3D`/`PROJECTION_VIEWER_DISTANCE` from `fourd.ts` unchanged.
- **Task 21:** Each part is rendered as a rigid "fat point," the same simplification `RevealStage.tsx`'s tracked-vertex marker already uses one level up: only the part's *center* anchor goes through `applyRevealRotation`/`projectTo3D`, and the same w-dependent scale factor `projectTo3D` derives for that center (`PROJECTION_VIEWER_DISTANCE / (PROJECTION_VIEWER_DISTANCE - w)`, read directly off the rotated anchor rather than re-derived) is applied as a uniform `<mesh scale>` on that part's own box/cylinder geometry. This is what produces PLAN.md's "different parts warp at different rates" tell without any new math: parts at different rest-x values pick up different w mid-rotation (proved directly in `chirality.test.ts`), so their scale factors diverge mid-turn and reconverge to a uniform 1 (and an exact x-mirror) exactly at 180°— confirmed by eye (see below), not just asserted.
- **Task 21:** Material is a plain `meshStandardMaterial` (warm skin-tone palette, `#caa27a`/`#e2c39a` — distinct from both the shapes' cool indigo/violet and the arrow's gold), not `materials.ts`'s hand-rolled shader system — the reveal stage's real `<directionalLight>`s already light this stage (`Experience.tsx`'s `SceneLights` turns on "rich lighting" from `cube` on, which includes `reveal`), so a real lit material was simpler than replicating the shader rig's light uniforms for a shape that doesn't need Task 7's animated-noise treatment. Satisfies PLAN.md's "a solid (non-wireframe) material" ask directly.
- **Task 21:** `ui/RevealControls.tsx`'s toggle button now cycles through 3 labels (`NEXT_VIEW_LABEL`, each naming the *next* view) instead of a binary flip; `state/store.ts`'s `toggleRevealView` action (name kept as-is despite no longer being a strict toggle, to avoid an unrelated rename across every caller/test) now calls a new `stageConfig.ts` helper `nextRevealView`/`REVEAL_VIEW_ORDER` rather than inlining a ternary. `ui/DimensionPanel.tsx`'s `RevealDimensionPanel` hides its tesseract-specific rows (tracked vertex ledger, edge count, cross-section extent, distance-from-projection) behind `revealView !== 'chirality'` — those numbers describe `TESSERACT_VERTICES`, a shape that isn't rendered at all in this view, so showing them would be actively misleading rather than just unused; a new `dimension-chirality-note` row explains the swap. Rotation (xw/yw) and slice-offset (w0) rows stay visible in every view, matching the existing precedent (Task 14's notes) that the slice offset already keeps updating in projection view even though it isn't visually expressed there either — same "shared state, not always shown" idea, just extended to a third lens.
- **Task 21:** Updated `e2e/reveal.spec.ts`'s view-toggle test (now asserts the full slice→projection→chirality→slice cycle, including a new `reveal-chirality-view.png` screenshot) and `store.test.ts`'s `toggleRevealView` test (now asserts the 3-way cycle) — both were written against the old binary toggle and would otherwise have silently stopped testing the third state. `e2e/instrumentation-panel.spec.ts` only needed its local `RevealState` type widened (still only toggles once in its own tests, so behavior there is unaffected).
- **Task 21:** New `e2e/chirality.spec.ts` (2 tests): one confirms the view is reachable via 2 toggle clicks, starts inert with no drag, and shows the panel's chirality-specific note instead of tesseract rows; the other drives a real half-turn drag through the browser using a two-step calibrate-then-correct technique (a small probe drag measures radians-per-pixel, then a corrective second drag lands within `toBeCloseTo(Math.PI, 1)` of an exact half-turn) rather than a hardcoded pixel constant — the camera never moves during this test (no button clicks), so the drag-plane raycast is a fixed affine map and this converges precisely, unlike Task 17's notes on a genuinely nonlinear pass/fail margin needing an empirical sweep. Confirms the achieved rotation matches `chirality.test.ts`'s exact-mirror math fed the real (not exactly-π) angle the drag produced. Per PLAN.md's verify step, also screenshotted before/mid-turn/after and reviewed by eye via a throwaway (uncommitted) script with a properly-calibrated exact-180° drag: at rest the fingers fan out clearly (recognizably hand-like); mid-turn (~72°) the fingers visibly collapse toward a tight vertical cluster as x rotates into w (exactly the expected "x→0 at 90°" compression, not a bug); at exactly 180° the thumb has swapped from the palm's left side to its right side — an unmistakable, legible mirror flip — confirmed against a fresh `npm run dev` instance, not just the committed test's `toBeCloseTo` tolerance.
- **Task 21:** Verified via `npm test` (105/105, including 4 new `chirality.test.ts` cases), `npm run lint`, `npm run build`, and the full Playwright suite (32/34 — the 2 failures are `richness.spec.ts`'s frame-rate floor and `validation-wiring.spec.ts`'s Stage 1 fail-cue timing race, both already documented pre-existing flakes from Tasks 7/15/18's notes; re-confirmed unrelated this session by `git stash`-ing every change and rerunning both against the exact Task 20 baseline commit, where they fail identically — not a regression). Also did the `npm run dev` visual check described above.

**Context-window check-in:** this session covered Task 21 end to end in one pass — reconciliation (confirmed the task branch sat exactly at the default branch's tip, no stale-base issue), reading PLAN.md/PROGRESS.md and the existing `RevealDrag`/`RevealStage`/`fourd.ts`/`materials.ts` code closely enough to reuse its patterns exactly rather than reinvent them, the math/scene/store/UI implementation, new unit + Playwright tests, a `git stash` sanity check confirming both remaining test failures pre-date this task, a manual `npm run dev` visual check (including a precisely-calibrated 180° screenshot) reviewed by eye, and this PROGRESS.md update. No `AskUserQuestion` was needed — PLAN.md's one open tunable for this task (where the demo sits in the flow) was resolved by the locked-decision cross-reference reasoning above, not a UX call needing the user's input. Conversation context was not auto-summarized during this session. Single-task session per CLAUDE.md — the next session should start fresh from Task 22 (closing beat + restart), which PLAN.md's own "new session recommended" marker (before Task 22) already called out as the start of the regression/polish phase, independent of both Stage 4 demonstrations built in Tasks 20-21.

- **Task 22 — how the player leaves Stage 4 (PLAN.md's own open call: "a rotation/drag-count threshold, or on a 'continue' click"):** went with a plain, ungated "continue" button (`ui/ClosingContinue.tsx`, always available once `revealWarmupActive` is false — no drag-count minimum), not a threshold. This matches this app's own established, repeated precedent against forced minimums: Task 18's "no artificial lock" locked decision, `SliceWarmupControls`' own doc comment ("no forced minimum play time... the least ambiguous way to satisfy 'skippable'"), and `RevealControls`' view toggle all already work this way for the same stage. A drag-count gate would be the first thing in this app to make the player do a specific number of actions before being allowed to leave, which isn't how anything else here treats "am I done."
- **Task 22:** No new store state needed — `advanceStage()` (reveal → closing) and `reset()` (back to Stage 1) already existed and were already fully covered by `store.test.ts`'s `advanceStage`/`reset` suites (including the stage-order walk that already asserted `reveal -> closing`), so this task is UI-only: two new components (`ui/ClosingContinue.tsx`, `ui/ClosingBeat.tsx`) plus small edits to `ui/HUD.tsx`, `scene/Experience.tsx`'s stale "'closing' gets its own scene in Task 15" comment, and `src/App.tsx`'s render list.
- **Task 22:** `scene/Experience.tsx`'s `StageGeometry` already fell through to `null` for `'closing'` (and `Grid.tsx` already skips it) — confirmed by reading, not assumed, that this was deliberate scaffolding rather than a gap: the closing beat needed no new 3D content, just the existing empty-but-lit void as a backdrop for the DOM overlay. Left that `default: return null` branch as-is, just fixed its comment (the "Task 15" reference was stale/wrong).
- **Task 22:** `HUD.tsx` renders every other stage's prompt as an instant cut — the closing line is deliberately the one exception ("fade in," per PLAN.md), so rather than adding a special-cased transition to the shared `HUD` component (which would need to *not* apply to every other stage), `HUD` now just skips its prompt row when `stage === 'closing'` and `ui/ClosingBeat.tsx` owns rendering that same text (`STAGE_CONFIG.closing.prompt`, not a duplicated string) with its own fade. `HUD`'s stage-label row ("CLOSING") still renders normally either way — `e2e/stage-shell.spec.ts`'s existing per-stage label assertion needed no changes.
- **Task 22:** `ClosingBeat`'s fade-in is a plain mount-triggered CSS opacity transition, split into an outer stage-gate component and an inner content component (`ClosingBeatContent`) specifically so the "reset to invisible" case is handled by unmounting rather than a `setState` call inside a `useEffect` keyed on stage — the latter tripped `eslint-plugin-react-hooks`'s `set-state-in-effect` check on the first pass (a different rule than the "modifying a value returned from a hook" class Tasks 7/9/10 documented, but the same spirit: let a plain remount do the state reset instead of fighting the effect graph). No forced auto-advance out of `closing` — restart is the only way out, exactly as PLAN.md asks.
- **Task 22:** `ClosingContinue`'s button placement took two tries — an initial top-right position collided with `DimensionPanel`'s tesseract-rotation readout (also top-right during `reveal`), caught by an actual `npm run dev`/Playwright screenshot look, not just by reasoning about CSS. Moved to bottom-left, the one screen corner nothing else on the reveal stage claims (`DimensionPanel` top-right, `CameraDirectionalControls` bottom-right, `RevealControls`+`DebugStageControls` bottom-center) — re-screenshotted to confirm no overlap before committing.
- **Task 22:** New `e2e/closing.spec.ts` (2 tests): the main flow drives reveal → click continue → assert `stage === 'closing'` via the dev-only `window.__dimensionsStore` hook (Task 14's precedent, same reasoning as `reveal.spec.ts`'s `revealState` helper — the reveal material's own idle shader animation makes pixel-diffing an unreliable proxy for state, and closing's text-fade has the same problem for a different reason: a screenshot taken at the wrong instant mid-fade would be flaky), confirms the closing line's opacity starts below 1 and reaches exactly 1 (proving it's a fade, not an instant appearance), then clicks restart and asserts both the visible HUD ("LINE") and the full store state (`stage`, `attempts`, `revealRotationXW`, `revealSliceW0`) are genuinely back to initial — not just that the stage field changed. A second test confirms the continue button is absent during the slicing warm-up (reached via a real `advanceStage()` call, not the `debug-stage-reveal` jump, since only real progression sets `revealWarmupActive` — see `store.ts`'s note) and appears once the warm-up is skipped.
- **Task 22:** Verified via `npm test` (100/100), `npm run lint`, `npm run build`, and the full Playwright suite (36/36 — no pre-existing flakes surfaced this run, unlike Task 21's session; possibly environment-dependent per that task's own note on `richness.spec.ts`'s frame-rate floor). Also did a manual `npm run dev`-equivalent visual check via a throwaway (uncommitted, deleted after) Playwright script/screenshot confirming the button placement fix and the closing screen's overall look (reviewed the image directly, not just asserted-in-test).

**Context-window check-in:** Task 22 was a short, single-focus session — read PROGRESS.md/PLAN.md's Task 22 section, explored the existing store/stage-config/UI-component patterns (confirmed `advanceStage`/`reset`/the `'closing'` stage/its camera framing already existed from earlier tasks), implemented two small new components plus three small edits, fixed one placement bug caught by an actual screenshot, and wrote/ran tests. No auto-summarization occurred; this was well within a single context window. The next session should start fresh from Task 23 (full-playthrough regression test).

- **Planning session (after Task 22, playtest + a small prototype flag, minimal code changed):** started as a request to test the current build — that surfaced a stale-branch issue worth recording separately (see below), and once re-tested against the real Task 22 tip, the user played it directly and gave four pieces of feedback. Three are sized as new tasks, inserted between Task 22 and the original Task 23 (renumbering the original Tasks 23-25 to 26-28 — same "insert before not-yet-started work" pattern as every prior insertion in this doc): **Task 23** (camera D-pad controls become available on every stage, including the currently-locked Stage 1, but with resistance/spring-back on stages that don't structurally support free movement, instead of either a hard clamp or no buttons at all), **Task 24** (a substantial instrumentation-panel redesign — continuous cursor tracking instead of drag-gated, plain-language labels leading with the math notation demoted to subtext, and richer per-stage live data: a cursor-projection dot on Stage 1's line, live x/y cursor position on Stage 2, and a Task-19-style tracked vertex on Stage 3 replacing the current post-drag decomposition percentages), and **Task 25** (a growth animation connecting Stage 1→2 and Stage 2→3, explicitly *not* played across Stage 3→Reveal, where new copy explains that a 4th dimension can't be grown into the way the first three could — but its shadow, via Task 14's existing projection view, can still be seen). See PLAN.md's three new "post-Task-22" locked-decision rows, their three rationale paragraphs, and each task's own deliverable/tunable text for the full detail — not duplicated here. The fourth piece of feedback (below) was small enough to act on immediately rather than defer.
- **Planning session — stale-branch issue found and fixed, not part of the four feedback items:** the session that ran Task 22 had, by the time this session started, left the local working branch pointed at the pre-Task-16 tip (`78b086e`) even though `origin/claude/dimensional-exploration-app-uj20jy` (the actual default branch) was already 7 commits ahead at `de6dac6` (through Task 22) — a `claude/task-22-w9vvzb` branch existed at the exact same SHA as the default branch tip, confirming Task 22 itself *had* been merged back correctly; the problem was purely this session's local checkout being stale, not a repeat of the "work stranded on an unmerged branch" failure mode CLAUDE.md's step 1 already warns about. First-pass testing was done against the stale `78b086e` tip and reported to the user as if it were current — caught only because the user said "I don't think you have the right build," not because the session's own reconciliation step caught it. Root cause: `git status`/`git log` were checked locally without ever running `git fetch`/`git ls-remote` against the specific default branch first, so a stale local ref never got the chance to look stale. Fixed by fetching every `claude/*` branch and fast-forwarding local to `origin`'s real tip before re-testing. Worth restating plainly for a future session even though CLAUDE.md's step 1 already says this: checking `git log` on the branch you were handed tells you nothing about whether that branch is current — only a fetch against the remote does.
- **Task 21 prototype flag (small, done immediately rather than deferred):** the user, after playing the chirality demo, called `ChiralityDemo.tsx`'s box-and-cylinder hand a placeholder that "sucks" and needs serious rework later, but asked for it to be *marked* as a prototype right now rather than rebuilt now. Added `ui/ChiralityPrototypeBadge.tsx` (a small top-left overlay, the one screen corner Task 22's own placement notes confirm nothing else on the reveal stage claims — visible only when `revealView === 'chirality'`) plus doc-comment flags in `math/chirality.ts` and a Task 21 tunables note in PLAN.md pointing at Task 27 (visual/tone polish, renumbered from the original Task 24) as where the real remodel should happen. No math/behavior changed — `npm run lint`, `npm test` (100/100), and `npm run build` all passed, plus a manual browser check (screenshot, badge renders with no collisions).
- **Could not push this session's commit (`fd860f0`) to `origin`:** this sandbox has no git credentials configured for `https://github.com` (`git push` fails with "could not read Username for 'https://github.com': Device not configured") — not a merge conflict, just no push access from this environment. The commit is real and sits on top of `de6dac6` (Task 22's tip) locally on `claude/dimensional-exploration-app-uj20jy`, but `origin/claude/dimensional-exploration-app-uj20jy` does **not** have it yet as of this session's end. The next session (or the user, from a machine with push access) needs to push this branch before trusting `origin`'s default branch to already contain this planning update — don't assume it's there without checking `git log origin/claude/dimensional-exploration-app-uj20jy` first, per this same doc's own repeated lesson about stale refs.

- **Task 23 — buttons render everywhere now, no more per-stage `cameraControlsEnabled` gate:** `ui/CameraDirectionalControls.tsx` dropped its `if (!cameraControlsEnabled) return null` early return entirely — the component now always renders its D-pad + zoom buttons, on every stage including Stage 1. `cameraFraming.ts`'s `CameraFraming` interface dropped the `cameraControlsEnabled: boolean` field for the same reason (nothing gates rendering anymore).
- **Task 23 — `azimuthRange`/`polarRange` repurposed, not replaced, per PLAN.md's own hint:** through Task 17 these two fields were optional and, when present, were passed straight through to `CameraControls`' own `minAzimuthAngle`/`maxAzimuthAngle`/`minPolarAngle`/`maxPolarAngle` — a hard library-level clamp a button press could never cross. They're now **required** on every stage (Stage 1 previously omitted them since it had no buttons) and mean something softer: the "free zone" a button can move within with zero resistance. `CameraControls` itself no longer receives any per-stage clamp at all — `Experience.tsx`'s `CameraRig` now always passes the library's own natural full range (`minPolarAngle={0}`, `maxPolarAngle={Math.PI}`, `minAzimuthAngle={-Infinity}`, `maxAzimuthAngle={Infinity}`) regardless of stage, and all per-stage restriction moved into userland: `cameraFraming.ts`'s new `resistedStep()` (shrinks a button's step once already past a boundary) and `easedTowardRange()` (one frame's spring-back toward the nearest edge), both pure and unit-tested directly (`cameraFraming.test.ts`, 13 new cases) rather than only through the browser. Stage 1 got a brand-new **zero-width** range (`[DEAD_ON_AZIMUTH, DEAD_ON_AZIMUTH]`/`[DEAD_ON_POLAR, DEAD_ON_POLAR]`, both `= (0, π/2)` — confirmed to match Stage 2's own asserted dead-on rest state, since both stages' `position`/`target` differ from their common target only by the same pure `+z` offset) — any movement at all counts as "outside," per PLAN.md's own tunable note flagging Stage 1 as "arguably any movement at all" deserving resistance. Stage 2 keeps `PLANE_TILT_RANGE = 1.0` unchanged (the free zone's width, not a hard cap anymore). Stage 3 on keeps the fully unclamped range, making both new functions permanent no-ops there (locked in directly by a `cameraFraming.test.ts` case) — confirmed identical to Task 17's original behavior by the full pre-existing Playwright suite passing unmodified for every Stage-3+ test.
- **Task 23 — why spring-back settles at the free-range *boundary*, not some separate "true rest" value:** `easedTowardRange(current, range, t)` eases `current` toward `clamp(current, min, max)` — i.e. the nearest edge of the same `azimuthRange`/`polarRange` `resistedStep` resists against, not toward a distinct rest constant. This was a deliberate reading of PLAN.md's verify text ("Stage 2 shows the same resistance-then-settle behavior **at its tilt boundary** instead of a hard clamp") — Stage 2 pressed exactly to (or slightly past) `PLANE_TILT_RANGE` and released settles back to `PLANE_TILT_RANGE` itself, not all the way back to dead-on `0`. This also happens to be exactly what keeps `e2e/validation-wiring.spec.ts`'s pre-existing Stage 2 tests passing completely unmodified: `tiltPlaneCamera`'s 3 button clicks + a fixed wait need the tilt to still be there by the time the drag fires, which only holds if settling stops at the boundary rather than sailing back through it to `0`. No separate "rest" field was needed anywhere — the boundary *is* the rest value wherever the two happen to coincide (Stage 1's zero-width range).
- **Task 23 — the gate that stops spring-back from fighting a stage-change transition (or a held button) is `controls.active`, not a timer:** camera-controls exposes a public readonly `.active` boolean ("returns true if the controls are active updating" — confirmed by reading `node_modules/camera-controls/dist/camera-controls.module.js` directly, not just its `.d.ts`) that's true exactly while its own internal damped transition hasn't yet converged — true for the whole duration of a `setLookAt(...,true)` stage-change transition, and true throughout a held button (each repeat tick reissues a fresh damped target, keeping it true until the last one finally catches up after release). `Experience.tsx`'s `CameraRig` gates its per-frame spring-back `useFrame` on `!controls.active`, so it never runs a competing "ease toward the free-range edge" pass while the library's own transition is still actually moving the camera for an unrelated reason. Confirmed this doesn't create a fight in practice: the full pre-existing e2e suite (which exercises exactly this kind of stage-change-into-a-resisted-stage sequence, e.g. cube → plane in `stage-shell.spec.ts`) passes unmodified. Avoided an arbitrary "wait N ms after a stage change" timer entirely, which would've been guessing at the library's own `smoothTime`.
- **Task 23 — a real runaway bug found via manual `npm run dev` testing, not caught by any test until after the fact:** the first working version of `resistedStep` read `controls.azimuthAngle`/`.polarAngle` (the *live*, still-damping-toward-its-target value) as "current position" every time a button fired. That's wrong: camera-controls' own `rotate(delta, ..., true)` computes its new target as `this._sphericalEnd.theta + delta` — the *pending* target (a private field, not exposed) — not `live + delta`. During a sustained hold (`RepeatButton`'s `HOLD_REPEAT_MS = 90`ms ticks, faster than the default damped transition can fully catch up between ticks), the live value lags the real pending target more and more the longer the hold continues, so resistance kept computing `excess` against a smaller, stale number — under-resisting every subsequent tick and letting the *true* pending target run away almost fully unresisted. Measured directly via `window.__cameraControls` in a running dev server (a throwaway, uncommitted Node+Playwright script, not `npm test`): Stage 2's azimuth reached **2.9 rad (past 165°!)** after just 1.5s of holding the "rotate right" button, then took over 2 seconds after release to fully settle back to `PLANE_TILT_RANGE = 1.0` — nothing in the automated suite would have caught this, since every Playwright test that presses buttons uses either single `.click()`s or the pre-existing `clickRepeatedly` helper (fixed short clicks with gaps, never a genuine sustained hold long enough to expose the lag). Fixed in `CameraDirectionalControls.tsx` by tracking the pending azimuth/polar target locally (module-level `pendingAzimuth`/`pendingPolar`, resynced from the live value only when `controls.active` is false — the one moment live *is* the pending target, so it's trustworthy) rather than ever reading `controls.azimuthAngle` directly inside `rotate()`. Re-measured after the fix: 1.5s of holding now reaches ~1.54 rad (not 2.9) and settles to exactly `1.0` within ~1.2s of release.
- **Task 23 — resistance formula swept from a rational to an exponential falloff, also found via the same manual dev-server testing:** even after the pending-target fix above, the *first* falloff tried (`1 / (1 + excess / RESISTANCE_DISTANCE)`, `RESISTANCE_DISTANCE = 0.5`) still let a sustained hold creep unboundedly — a rational falloff only decays like `1/excess`, so cumulative position under continuous holding grows like `sqrt(time)`, slower than unresisted but never actually bounded (a Node-only numeric simulation, not committed, confirmed this: ~30 ticks reached `azimuth ≈ 3.7` even at the stiffest rational value tried, `RESISTANCE_DISTANCE = 0.05`). Switched to an exponential falloff (`exp(-excess / RESISTANCE_DISTANCE)`, decaying like `1/e^excess`) and swept `RESISTANCE_DISTANCE` down from `0.5` to `0.15` the same empirical way Task 17 swept `PLANE_TILT_RANGE` — the same Node simulation showed exponential falloff at `0.15` keeps even a 40-tick (3.6s) hold within roughly half a radian of the boundary, while a single unresisted step still applies right at the boundary itself (so the very first press past it reads as a real, felt nudge, not instantly-mush). Landed on `0.15`. See `cameraFraming.ts`'s `RESISTANCE_DISTANCE`/`resistedStep` doc comments for the full before/after numbers; `cameraFraming.test.ts`'s "shrinks a step" case was updated to assert the new exponential factor (`exp(-1)` at exactly one `RESISTANCE_DISTANCE` past the boundary) rather than the old rational one.
- **Task 23:** `e2e/orbit.spec.ts`'s two per-stage tests were rewritten, not just patched — the old Stage 1 test asserted `not.toBeVisible()` on the camera-controls div (now false, buttons always render) and the old Stage 2 test asserted a hard hit-a-wall-at-exactly-`1.0` clamp after 12 quick clicks (still passes as an *outcome*, since quick clicks mostly stay within the free zone and land right at its edge, but doesn't exercise resistance itself). Both new tests hold a button down via real `page.mouse.down()`/`page.mouse.up()` (not `.click()`) so a mid-hold read/screenshot lands while genuinely still off rest: Stage 1 confirms `camera-controls` is now visible, holds `camera-control-right` for 250ms and confirms both the reported azimuth and a canvas screenshot differ from the at-rest baseline (Stage 1's line material is static, so pixel-diffing is reliable here per Task 5/6's existing precedent), releases, then confirms `waitForCameraSettled` converges back to `(0, π/2)` within its existing 3s poll budget. Stage 2 holds for 1.5s (long enough to clear the boundary with real margin under the tuned resistance) and asserts the mid-hold azimuth is strictly `> PLANE_TILT_RANGE` — something a true hard clamp could never produce — then confirms it settles back to `PLANE_TILT_RANGE` (not `0`) after release. Both new tests were run via `--repeat-each=4` (8 runs total) with zero failures before being trusted.
- **Task 23:** `playwright.config.ts`'s previously-hardcoded `executablePath: '/opt/pw-browsers/chromium'` (documented in Task 2's notes as specific to that sandbox) doesn't exist in this session's worktree environment at all — no `/opt/pw-browsers` directory, and a stale unrelated `npm run dev` process from the *shared checkout* (not this worktree) was also found squatting on port 5173 early in this session, serving pre-Task-23 code and silently making the first attempted Playwright run look green against the wrong build (caught by rereading the diff, then killing that process and rerunning — the same "verify against a fresh state, don't trust a running server's provenance" lesson the planning session's stale-branch note already gives for git branches, just for a dev server instead). Fixed the browser path with a small existence check (`existsSync`) that falls back to `undefined` — letting Playwright resolve its own normally-installed browser — when the pinned sandbox path isn't there, rather than hard-failing; installed a fresh Chromium via `npx playwright install chromium` for this session. Does not change behavior in the original documented sandbox (still uses the pinned path when present), so this is additive, not a regression risk for a future session that does have `/opt/pw-browsers`.
- **Task 23:** Verified via `npm test` (118/118, 13 new in `cameraFraming.test.ts`), `npm run lint`, `npx tsc --noEmit`, `npm run build`, and the full Playwright suite (36/36, run clean multiple times; `e2e/orbit.spec.ts` specifically re-run at `--repeat-each=3`/`--repeat-each=4` with zero failures). Two already-documented pre-existing flakes surfaced across repeated full-suite runs this session — `validation-wiring.spec.ts`'s Stage 1 fail-cue timing race (Task 21's notes) and `richness.spec.ts`'s frame-rate floor (Task 7's notes) — both re-confirmed as pre-existing and unrelated by checking out this session's *unmodified* baseline (`git checkout HEAD -- <camera files>`, i.e. the exact Task 22 tip) and reproducing the identical failure there too, then restoring and diff-confirming nothing was lost. Also did the required `npm run dev` look (not just tests): a throwaway, uncommitted Node+Playwright script captured screenshots of Stage 1 at rest / mid-press / settled and Stage 2 at rest / held-well-past-boundary / settled, reviewed each by eye — Stage 1 visibly nudges then returns to a pixel-identical rest frame; Stage 2 visibly reveals its depth/edge under a sustained hold and settles to a comfortably-legible ~57° tilt with no button-panel collisions against `DebugStageControls`/`DimensionPanel`. Zero console/page errors throughout every manual and automated check.
- **Task 23 — pushed straight to `origin`'s default branch, unlike the previous session:** this session's worktree branch (`worktree-agent-abff3af7f7676df66`) started exactly at the default branch's real tip (`f3f7c81`, confirmed via `git ls-remote origin` against `git remote show origin`'s reported `HEAD branch` before any work began — no stale-branch issue). Rather than merging locally and pushing a separate step, this session pushed the commit directly onto the remote branch ref: `git push origin worktree-agent-abff3af7f7676df66:claude/dimensional-exploration-app-uj20jy`, which succeeded as a clean fast-forward (`f3f7c81..7a13af2`) — confirmed via `git ls-remote origin claude/dimensional-exploration-app-uj20jy` showing `7a13af2` immediately after. Worth flagging plainly since it contradicts CLAUDE.md's documented expectation and the previous two sessions' notes above (both of which hit "could not read Username for 'https://github.com': Device not configured"): **this particular sandbox instance did have git push credentials configured against `origin`**, so push access apparently isn't a fixed property of this project/environment overall — it varies session to session (or worktree to worktree), and a future session shouldn't assume either way without just trying it once, per CLAUDE.md's own instruction.

**Context-window check-in:** this session covered Task 23 end to end in one pass — reconciliation (confirmed no unmerged branch work, worktree already sat at the default branch's real tip), reading PLAN.md/PROGRESS.md, reading `camera-controls`' actual library source (not just its `.d.ts`) to understand `rotate()`/`rotateTo()`/`.active`'s real semantics before designing around them, the store/scene/UI implementation, 13 new Vitest cases, a from-scratch rewrite of two `orbit.spec.ts` tests, and — the bulk of the session's real time — two rounds of manual `npm run dev` + throwaway Playwright/Node scripts that caught a genuine runaway-resistance bug (a stale-live-value read) and an under-tuned falloff shape, neither of which any written test would have caught since none of the existing or new automated tests exercise a multi-second sustained button hold. Also cleaned up a stale dev-server process from the shared checkout that was silently serving the wrong build for the first verification pass, and fixed `playwright.config.ts`'s hardcoded browser path for this session's browser-less worktree environment. Conversation context was not auto-summarized during this session, though it ran long relative to most single-task sessions in this doc (the two rounds of empirical resistance-tuning added real back-and-forth beyond a typical implement-and-verify task). Single-task session per CLAUDE.md — the next session should start fresh from Task 24 (instrumentation panel redesign), which PLAN.md's own "new session recommended" marker (before Task 24) already called out as a distinct subsystem from the camera work here.

- **Task 24 — no `AskUserQuestion` tool available in this session's environment:** PLAN.md's own tunable note for Stage 3's tracked-vertex semantics explicitly says this needs a real decision from the user, not a guess, and calls out `AskUserQuestion` by name as the way to get it. This session's tool list didn't include it (confirmed via `ToolSearch` against both the deferred-tool list and a broad keyword search — genuinely absent, not just deferred). Implemented everything else first (Stages 1-2's continuous cursor tracking, the panel's plain-language/notation hierarchy inversion, the `maxWidth` layout fix), then asked the question as plain text in a normal response and stopped, leaving Stage 3 unbuilt, rather than guessing. The coordinator relayed the user's answer back in a follow-up message ("camera-relative... confirmed as the intended reading") — functionally the same result `AskUserQuestion` would have produced, just routed through the coordinator instead of a structured tool call, and worth recording plainly here so a future session doesn't misread "no `AskUserQuestion` call in this transcript" as "the decision was guessed."
- **Task 24 — Stage 3's tracked-vertex is camera-relative, per the user's confirmed answer:** `math/cameraRelative.ts`'s `toCameraRelative(worldPoint, camera)` transforms a world-space point into the camera's own view frame (`Vector3.applyMatrix4` with `camera.matrixWorld`'s own freshly-recomputed inverse — deliberately not the renderer-maintained `camera.matrixWorldInverse`, which only refreshes during the renderer's own `render()` call and could otherwise read stale). `scene/trackedCubeVertex.ts` picks an arbitrary cube corner (`SHAPE_POSITION.cube + (1.25,1.25,1.25)`, the far corner from the room-corner origin — arbitrary, same reasoning as Task 19's tesseract-vertex pick) as the point tracked. `scene/TrackedCubeVertexTracker.tsx` (mounted unconditionally in `Experience.tsx`, active only on `stage === 'cube'`) recomputes this every frame and writes it to a new store field `trackedCubeVertexCamera`, the same boundary-crossing pattern `liveDragVector`/`liveCursorPoint` already use — `ui/DimensionPanel.tsx`'s cube-stage x/y/z rows read from it instead of the old per-drag `liveDragVector`, so they're now continuously live (no drag needed) exactly like Stages 1-2. Confirmed by eye (not just the automated suite): orbiting the camera 6 clicks via the D-pad on a running `npm run dev` visibly changed the panel's cyan numbers while the marker dot stayed pinned to the same physical cube corner — screenshots before/after showed clearly different readings (e.g. `x: 0.22 → -1.64`) for a camera move that never touched the cube itself.
- **Task 24 — a real R3F bug found only by looking at the running app, not by any test:** the first pass of `TrackedCubeVertexTracker.tsx` called `useFrame(callback, 1)`, intending the `1` to make this callback run *after* `<CameraControls>`'s own default-priority update each frame. That's not what a positive `useFrame` priority means in `@react-three/fiber` — passing any `priority > 0` tells R3F "I'm taking over rendering myself" and switches off its own automatic `gl.render(scene, camera)` call every frame (confirmed directly in `node_modules/@react-three/fiber`'s source: `internal.priority` gates the auto-render line, and any positive-priority subscriber increments it). Since nothing in this app calls `gl.render()` manually, this silently blanked the *entire* canvas on *every* stage, not just Stage 3 — caught only by an actual `npm run dev`-equivalent look (a throwaway Playwright script screenshotting Stage 1, which should have been trivially fine and wasn't), not by `npm test`, `npm run lint`, `npx tsc`, `npm run build`, or the full Playwright suite, all of which passed clean throughout because none of them assert that the canvas actually contains visible pixels beyond a couple of narrow pixel-diff cases that happened not to exercise this path in a way that failed loudly (Playwright's own screenshot-based tests for *other* stages weren't run again after this specific edit until the manual check). Fixed by dropping the priority argument entirely (default `0`, same as every other `useFrame` in the codebase) — `toCameraRelative`'s own `camera.updateMatrixWorld()` call already guarantees a correct-for-this-frame transform regardless of subscriber ordering, so the priority trick was never actually needed. Re-verified with a fresh, non-stray dev server (see below) that the cube, its cyan marker, and every other stage's geometry all render again. Worth remembering for any future `useFrame` call in this codebase: **never pass a positive priority unless something is deliberately taking over the render call**.
- **Task 24 — stray dev servers actively caused false signals twice this session, same lesson Task 23's notes already give for a different symptom:** while manually verifying the bug above, a leftover `vite dev --port 5173` process from an *earlier* verification step in this same session (never explicitly killed after use) was still running when a second one was started — `ps aux` showed two/three overlapping `vite`/`npm exec vite` processes at once. A fresh `npx playwright test` run (which reuses an already-running server on port 5173 per `playwright.config.ts`'s `reuseExistingServer: !CI`) kept passing throughout, but ad-hoc manual scripts pointed at `localhost:5173` got inconsistent, occasionally broken results depending on *which* stray process happened to be serving that request. Fixed by `ps aux | grep 'vite dev'`, killing every matching PID, and starting exactly one fresh server before trusting any manual check again. Same root lesson Task 23's PROGRESS.md notes already state for a *different* stale-process symptom (wrong build served instead of blank rendering) — worth restating because this session hit it independently rather than having read that note closely enough to avoid it pre-emptively: **always confirm zero stray `vite`/`npm exec vite` processes before trusting any manually-launched browser check against `localhost:5173`.**
- **Task 24 — the persistent cursor marker broke two pre-existing pixel-exact "returns to rest" tests, fixed at the test level:** `e2e/arrow-drag.spec.ts`'s Stage 1 drag test and `e2e/validation-wiring.spec.ts`'s Stage 1 fail-cue test both captured an `atRest` canvas screenshot *before any pointer movement at all*, dragged, and asserted the canvas returned to byte-identical `atRest` afterward. Task 24's new `CursorMarker.tsx` legitimately renders a persistent gold dot wherever the cursor last landed (by design — "always live," per PLAN.md), so once any test's drag involves real pointer movement, the marker no longer matches its pre-movement absence. Fixed both tests by parking the pointer at one fixed, known point (`canvasCorner`, an existing "off the object" reference point from `arrow-drag.spec.ts`, duplicated into `validation-wiring.spec.ts` too) *before* capturing `atRest` and returning to that exact same point before the final comparison — isolates what these tests actually check (the drag arrow and camera state) from the new, separately-live marker. No other pixel-exact "back to rest" assertions in the suite were affected (checked via `grep` across every `e2e/*.spec.ts` file for `toEqual(atRest`/`canvasSnapshot` patterns).
- **Task 24 — scope call: left `RevealDimensionPanel` (Task 19's Stage 4 panel) untouched beyond the shared `maxWidth` fix, didn't apply the plain-language hierarchy there too.** PLAN.md's locked-decision row phrases the redesign as "the Task 15/19 panel," and its hierarchy-inversion sentence has no explicit stage qualifier, so a case could be made either way. Read it narrowly: the deliverable's own body text scopes every piece of *new* behavior to "Stages 1-3" specifically, Stage 4's rotation/slice/tracked-vertex fields are already continuously live (no `liveDragVector`-style null-at-rest problem to fix there), and rewriting Task 19/21's already-tested, already-shipped reveal-panel rows risked real regression for a stage this task wasn't otherwise touching. If the user wants Stage 4's panel restyled to match, that's a small, well-scoped follow-up now that `PanelRow` already exists as a shared component to reuse.
- **Task 24 — Stage 2 also got a cursor marker, not just Stage 1 (left as this task's own call per PLAN.md's tunable note):** `CursorTracker.tsx` already produces the same `{x, y}` shape for both stages, so extending `CursorMarker.tsx` to draw on the plane too cost nothing extra and reads consistently with Stage 1's "see your own shadow" idea.
- **Task 24 — the distance-formula/orthogonality rows stay drag-gated (`—` at rest), unlike the axis-ledger rows above them:** `evaluateAttempt`'s ratio is inherently a function of a *drag vector* (two points, start and current), not a single live position — there's no meaningful "continuous" version of "how far did you drag" without an actual drag in progress, so these two rows were restyled (plain-language primary, notation subtext) but not made continuous, and a new second Stage 3 test (`e2e/instrumentation-panel.spec.ts`) explicitly separates "the tracked corner is live with no drag" from "a real drag still updates the (separate) attempt-feedback rows" so the two different liveness stories don't get conflated in one assertion block.
- **Task 24:** Verified via `npm test` (127/127 — 3 new `setLiveCursorPoint`/`setTrackedCubeVertexCamera` store cases, 4 new `cameraRelative.test.ts` cases), `npm run lint`, `npx tsc --noEmit`, `npm run build`, and the full Playwright suite (38/38 — 1 new Stage-3 test beyond the prior 37, `arrow-drag.spec.ts`/`validation-wiring.spec.ts` both updated for the persistent-marker fix above). Also did the required `npm run dev`-equivalent look across every stage with a fresh, non-stray dev server (see the stray-process note above): Stage 1's marker dot visually tracks the cursor and matches the panel's reported `x`; Stage 2 shows both `x`/`y` live; Stage 3's cube renders correctly with its cyan marker on a real corner, and the readout visibly changes after orbiting via the D-pad while the marker itself stays pinned to that same physical corner; the reveal stage's chirality-view note now wraps inside the capped panel width with no header overlap. Zero console/page errors throughout.

**Context-window check-in:** this session ran long relative to most single-task sessions in this doc — it covered Task 24 in two phases separated by a genuine stop-and-wait for user input (no `AskUserQuestion` tool, so the question went out as a plain-text response and the session paused an entire turn for the coordinator to relay the answer back), then a second phase implementing Stage 3 that included tracking down a real, non-obvious R3F rendering bug (the `useFrame` priority issue above) that produced no error of any kind and was only visible by actually looking at a screenshot. Conversation context was not auto-summarized. Single-task session per CLAUDE.md, despite the two-phase structure — the next session should start fresh from Task 25 (dimension-growth transitions), which PLAN.md's own "new session recommended" marker (before Task 25) already flags as the riskiest of the three post-Task-22 tasks and a strong candidate for the same "split if it doesn't converge" treatment Tasks 7/10/13/14 got.

- **Task 25 — one shared box-wireframe interpolation instead of bespoke geometry per boundary:** every one of Stages 1-3's shapes (`shapePositions.ts`, Task 16's positive-octant repositioning) shares one corner pinned at the world origin — the line is a box with two zero-extent sides, the plane one, the cube none. `math/growth.ts`'s `boxWireframeEdges(max)` always returns exactly 12 `[start, end]` edge pairs for an axis-aligned box from the origin to `max`; interpolating just `max` between two stages' own values (`GROWTH_MAX_CORNER`) and redrawing that box every frame is what makes one function serve both the 1->2 "sweep a perpendicular edge" transition and the 2->3 "extrude, and resize the base down to the cube's smaller footprint" transition, with the degenerate line/plane start/end shapes falling out for free rather than needing a special case. Verified this by dedup-testing against `stageConfig.ts`'s own `STAGE_SHAPE_COUNTS` (1/4/12 edges) — see `growth.test.ts`.
- **Task 25 — `GROWTH_MAX_CORNER`'s sizes are deliberately duplicated, not refactored out of `{Line,Plane,Cube}Stage.tsx`:** those three files' own geometry constructors (`BufferGeometry` from two points, `PlaneGeometry(3,3)`, `BoxGeometry(2.5,2.5,2.5)`) are the actual source of truth; `growth.ts` just documents that it's reading the same numbers rather than importing a shared constant. A scope call, not an oversight — refactoring three already-shipped, already-tested stage files into pulling their geometry size from a new shared module was more change surface than this task's own deliverable needed, for a value that isn't expected to drift (Stages 1-3's shapes are visually locked in from Task 16 on). Flagged in `growth.ts`'s own doc comment so a future task that resizes any of those three shapes knows to update `GROWTH_MAX_CORNER` too.
- **Task 25 — a real animation-feel bug found only by looking at actual screenshots, not by any test:** the first pass used a cubic ease-*out* (`1-(1-t)³`) on the theory that a fast start reads as "something actively extending itself." Wrong in practice — ease-out that aggressive is so front-loaded that `t=0.5` (linear/elapsed-time halfway) already produces `0.875` of the visual distance, so by roughly a third of the way through `GROWTH_DURATION` the shape already looked essentially fully grown, with only an imperceptible sliver left to creep for the rest of the duration. Every automated check passed throughout (the geometry genuinely was interpolating, just badly distributed perceptually) — this only surfaced by actually looking at sampled mid-transition screenshots (`e2e/screenshots/manual-growth-*-t1/t2/t3.png`, a throwaway diagnostic spec, not committed) and seeing the "mid" frame look indistinguishable from "settled." Switched to smoothstep (`3t²-2t³`, symmetric, slow at both ends, fastest through the middle) — re-checked the same way, and the four sampled frames now show a clearly partial rectangle/box at each point, not a near-instant pop. `growth.test.ts`'s easing tests were rewritten to assert smoothstep's actual properties (exact midpoint at `t=0.5`, slower than linear near each edge, symmetric) rather than the old ease-out's front-loading, which would otherwise have kept passing against the wrong curve.
- **Task 25 — a real, reproducible full-suite-only timing flake, fixed with delta clamping (not just a test workaround):** `e2e/growth-transition.spec.ts`'s Stage 2->3 test failed intermittently (2 of ~5 full `npx playwright test` runs, always this test, never Stage 1->2) with `growthTransition` already `null` immediately after the click that should have started it — i.e. the animation had already fully completed within the click-to-first-read window. Isolated via a throwaway timing script: under the full suite's default 7-worker parallelism (several headless Chromium instances rendering WebGL concurrently, vs. this sandbox's 14 real cores), `useFrame`'s real `delta` can occasionally be large enough for one single frame to carry more simulated time than the animation's whole `GROWTH_DURATION` — the growth-animation equivalent of a physics "spiral of death." Bumped `GROWTH_DURATION` from an initial `0.5` to `0.6` for a bit more margin, but the real fix is `GROWTH_MAX_FRAME_DELTA = 0.05` (`math/growth.ts`): `StageGrowthTransition.tsx` clamps `delta` to this before accumulating it, guaranteeing at least `0.6 / 0.05 = 12` real frames render before the animation can finish no matter how stalled any individual frame's wall-clock delta was. This isn't just a test accommodation — the same clamp also protects a real player who backgrounds the tab mid-transition and returns from seeing the growth animation "teleport" to its end state. Confirmed fixed via 3 consecutive clean full-suite runs (42/42 each) after the clamp, versus 2 failures in 2 prior full-suite runs before it.
- **Task 25 — the destination stage's collider and Stage 3's tracked-vertex marker are also gated on `!growthTransition`, not just its visible geometry:** `ArrowDrag.tsx`'s per-stage collider mesh and `TrackedCubeVertexTracker.tsx`'s marker/readout both now check `growthTransition` (via the same `growthTransition.to === stage` reasoning `Experience.tsx`'s `StageGeometry` uses) and stay inert until the animation finishes — confirmed by eye in the plane->cube screenshots (`manual-growth-2-plane-to-cube-t1/t2/t3.png`): no tracked-vertex dot appears on the still-growing wireframe, only once the real `CubeStage` takes over at `t4`. Without this, a player could start a drag, or see the tracked-corner readout update, against a shape that visually isn't fully there yet.
- **Task 25 — the Stage 3->Reveal copy names both "shadow" and "projection" explicitly, not just one:** `stageConfig.ts`'s `reveal` prompt now reads "...you just can't point at it, so there's no growing into it the way the line grew into the plane, and the plane into the cube. What you can see is its shadow: a projection, the same way a 3D object casts a 2D one." Naming "projection" specifically (not just "shadow") ties directly to `ui/RevealControls.tsx`'s actual toggle-button label ("Show projection"), per PLAN.md's own instruction that the metaphor should arrive "by name before the player meets it" rather than be left for them to infer from the button alone.
- **Task 25:** Verified via `npm test` (148/148 — 12 new `growth.test.ts` cases, 7 new `growthTransition`/`finishGrowthTransition` store cases), `npm run lint`, `npx tsc --noEmit`, `npm run build`, and the full Playwright suite (42/42, repeated 3x clean after the delta-clamp fix above — 4 new tests in `e2e/growth-transition.spec.ts` covering both real growth transitions with store-state + screenshot assertions, that `setStage` debug jumps never trigger one, and the Stage 3->Reveal boundary's new copy with provably no animation). One unrelated pre-existing flake surfaced once in `orbit.spec.ts` (a Task 17/23 test, untouched by this task) during a full-suite run — reran clean 3x in isolation and once more as part of a full suite, consistent with this being the same category of sandbox-CPU-contention flakiness Task 7/23's notes already document, not a regression from this task's changes. Also did the required look-at-the-actual-result pass beyond automated screenshots: sampled 4 frames each through both real transitions plus the Stage 3->Reveal copy screen via a throwaway diagnostic Playwright spec (not committed), reviewed by eye — see the easing-bug note above for what that caught.

**Context-window check-in:** this session covered exactly Task 25, single-task per CLAUDE.md, with no auto-summarization noticed. It ran a bit long relative to a typical single-task session because the risk PLAN.md flagged up front (novel geometry-morph animation code) materialized as two real, non-obvious problems that only showed up empirically rather than as any test failure at first pass — the ease-out easing making the animation perceptually front-loaded (caught only by looking at actual sampled screenshots) and the delta-time single-frame-swallows-the-whole-animation race under full-suite CPU contention (caught only by a full-suite rerun, not the isolated spec file, which passed 12/12 on its own). Both were root-caused and fixed properly rather than patched around at the test level. The task converged in one sitting — no split was needed — but a future session picking up Task 26 (full-playthrough regression) should know this task's own new `e2e/growth-transition.spec.ts` is the newest, least-battle-tested spec in the suite and worth an extra look if anything around it seems flaky again.

- **Task 26 — one test, real progression throughout, matching PLAN.md's deliverable literally:** `e2e/full-playthrough.spec.ts` scripts Stage 1 pass (Continue) -> Stage 2 pass (tilt via the on-screen button first, since Task 17 rests it dead-on — Continue) -> Stage 3 two failed drags (deliberately under `CUBE_ATTEMPTS_BEFORE_REVEAL = 3`, so the auto-advance path is never hit — this test exercises "give up" specifically) -> the "I understand" button (real `advanceStage()`, not `debug-stage-reveal`, so `revealWarmupActive` genuinely goes `true` and the Stage 3->Reveal boundary genuinely skips the growth animation — see Task 20/25's notes on why `setStage` bypasses both) -> the 3D slicing warm-up (one drag, then skip) -> the tesseract's slice view (one drag) -> toggle to projection -> toggle to chirality (one drag) -> "I've seen enough" -> the closing fade -> restart, with a screenshot at every stage/beat (11 total, `full-playthrough-01` through `-11`). Helpers (`canvasCenter`/`dragFrom`/the store-state reader/`waitForGrowthTransitionCleared`) are deliberately copy-pasted from `validation-wiring.spec.ts`/`growth-transition.spec.ts`/`cube-decomposition.spec.ts`/`slice-warmup.spec.ts`/`chirality.spec.ts`/`closing.spec.ts` rather than factored into a shared module, matching this codebase's own established per-spec-file convention (every one of those files already keeps its own copies).
- **Task 26 — a real, currently-shipped bug found and fixed, not a pre-existing flake:** the first full run's screenshot of the tesseract slice view (`full-playthrough-06-reveal-slice.png`) showed `HUD.tsx`'s reveal-stage prompt (Task 25's longer "shadow"/"projection" copy, now long enough to wrap to 3 lines) running directly under `DimensionPanel.tsx`'s fixed top-right corner — the header's text and the panel's readout rows visibly overlapped mid-sentence. Confirmed this is a real bug in the already-shipped app, not an artifact of this new test: `HUD.tsx` was never touched by this session before finding it, and the exact same overlap reproduces in `e2e/screenshots/instrumentation-stage4-slice.png`/`reveal-slice-view.png` (pre-existing spec files, regenerated unmodified by the same full-suite run). Root cause: `HUD.tsx`'s prompt `<div>` had no `maxWidth` at all — at the container's own `width: 100%` (the full canvas width), a long wrapped prompt runs edge to edge, colliding with `DimensionPanel`'s `right: 1.5rem`/`maxWidth: 14rem` corner. This is the mirror image of the bug Task 24 already fixed once (there, the *panel* was growing wide enough to overlap the header; here, the *header* wraps wide enough to run under the panel) — Task 24's fix only capped the panel's own width, which does nothing for this direction. Fixed by giving `HUD.tsx`'s prompt element its own `maxWidth: '40rem'` plus `margin: '0.5rem auto 0'` (centers the now-narrower box itself, not just the text within an unbounded box) — confirmed by eye post-fix (`full-playthrough-06-reveal-slice.png` re-generated) that the same long copy now wraps to 5 shorter lines comfortably clear of the panel at the default 1280px test viewport, with no visible change to any shorter stage's prompt (Stage 1/2/3's single-line prompts were never close to the panel regardless). Every existing test that screenshots the reveal stage's header (`instrumentation-panel.spec.ts`, `reveal.spec.ts`) still passes unmodified.
- **Task 26 — one screenshot-timing lesson from this session's own test, not a product bug:** the first draft's post-restart screenshot (200ms after clicking "Start over") showed Stage 1's line visibly tilted, not perfectly horizontal — looked alarming until checked directly: restart re-triggers the same animated `setLookAt(..., true)` camera transition every other real stage change already uses (per `cameraFraming.ts`/`Experience.tsx`'s `CameraRig`), and 200ms isn't enough for it to settle back to Stage 1's dead-on rest framing after coming from wherever the camera was left in Stage 4. Confirmed empirically with a throwaway Node+Playwright script (killed a stray dev server left over from an earlier manual check first, per Task 23/24's own repeated lesson on stray `vite dev` processes causing false signals): visibly tilted at 200ms, fully settled and pixel-identical to the pristine Stage 1 screenshot by ~1s. Bumped the final wait to 1000ms — not a source change, just the test's own screenshot timing.
- **Task 26:** Verified via `npm test` (148/148, unchanged — no new unit tests, this task is Playwright-only per PLAN.md's deliverable), `npm run lint`, `npx tsc --noEmit`, `npm run build`, and the full Playwright suite run clean 4 times total across the session (43/43 each time — 42 pre-existing + the 1 new test), plus 5 concurrent repeats of the new test alone (`--repeat-each=5`, all 5 workers running simultaneously — deliberately the same CPU-contention condition that surfaced Task 25's growth-transition flake — zero failures). None of the three previously-documented pre-existing flakes (the `validation-wiring.spec.ts` Stage 1 fail-cue timing race, the `richness.spec.ts` frame-rate floor, and the `orbit.spec.ts` CPU-contention flake Task 25's notes mention) surfaced in any of this session's runs — noted as "didn't reproduce this session," not "fixed," since they're already documented as environment-dependent rather than deterministic. Also did the required actual-look pass beyond automated assertions: read all 11 saved screenshots directly (not just asserted-in-test) — Stages 1-3 read correctly (dotted line, graph-paper plane, cube in its grid corner with a live tracked-vertex readout), the cone warm-up and both tesseract views read correctly, the chirality demo's placeholder hand (already flagged as a Task 21/27 prototype, `ChiralityPrototypeBadge` visible as expected) responds live to its drag, and the closing beat/restart both read correctly — this pass is what caught the HUD/panel overlap bug above, which no automated assertion in the suite would have caught (same "only caught by looking" pattern Tasks 23/24/25's own notes already describe repeatedly for this codebase).
- **Task 26 — one worktree-environment note, not a code issue:** this session's worktree had no `node_modules` and no `/opt/pw-browsers` pinned Chromium (same "fresh worktree, different sandbox" situation Task 23's notes already describe) — ran `npm install` and `npx playwright install chromium` before anything else; `playwright.config.ts`'s existing `existsSync` fallback (added in Task 23) handled the missing pinned path with no code change needed.
- **Task 26 — nothing deferred to Task 27 beyond what PLAN.md's own task boundary already scopes there:** no other rough edges surfaced during this playthrough beyond the HUD/panel overlap (which was fixed, not deferred). The already-known, already-documented items — the chirality demo's placeholder hand (Task 21/27), the projection view occasionally clipping the canvas edge at some rotation angles (Task 14/20), and the general visual/tone/copy pass (Task 27's whole deliverable) — are all pre-existing and explicitly out of scope for this task; none of them are new findings from this session.

**Context-window check-in:** this session covered exactly Task 26, single-task per CLAUDE.md, with no auto-summarization noticed. It stayed well within a normal single-task session — reconciliation (confirmed the worktree started exactly at the default branch's real tip, `9cc45e5`, no stale-branch issue), reading PLAN.md/PROGRESS.md and the six most relevant existing e2e specs closely enough to reuse their exact helper patterns and testids rather than reinvent them, writing and repeatedly verifying the one new test (including a deliberate 5-way concurrent repeat to stress-test for the same class of CPU-contention flake Task 25 hit), finding and properly fixing one real pre-existing layout bug via actually reading the screenshots rather than trusting green tests alone, and this PROGRESS.md update. No `AskUserQuestion` was needed — PLAN.md's Task 26 deliverable left no open UX/design tunables, only implementation judgment calls (exact drag deltas, exact stage-3 fail count, wait/settle timing), all resolved by reusing this codebase's own already-established empirical values rather than inventing new ones. This session's commit was pushed straight to `claude/dimensional-exploration-app-uj20jy` (the default branch) on `origin` as a clean fast-forward (`9cc45e5..249c154`) — this sandbox instance did have git push credentials configured, consistent with Task 23/24/25's own notes that push access varies session to session rather than being a fixed property of the environment. The next session should start fresh from Task 27 (visual/tone polish, optional) or Task 28 (deploy, optional) per PLAN.md — both are explicitly optional and neither was touched here.

## Playtest follow-up: Task 25's growth-transition duration (direct user feedback, post-Task-26)

- ✅ Slowed `GROWTH_DURATION` (Task 25's own already-flagged tunable — PLAN.md never chose a default) from `0.6` to `2.5` seconds — `claude/shape-animation-dimensions-c2khnp` @ `240e409`. The user played the shipped Task 22-26 build and reported the Stage 1->2 / 2->3 growth animation was so fast it read as an abrupt pop rather than a visible grow, and that they hadn't even consciously registered it was there at all. `math/growth.ts`'s `GROWTH_DURATION` doc comment and the two consuming e2e specs (`e2e/growth-transition.spec.ts`, `e2e/full-playthrough.spec.ts`) were updated together — the `growthTransition`-cleared poll timeout in both files went from `3000ms` to `6000ms` so the new 2.5s duration still has comfortable headroom under it, same margin ratio the original `0.6s`/`3000ms` pairing had. No change to `easeGrowth`'s smoothstep curve itself, `GROWTH_MAX_FRAME_DELTA`, or any geometry — purely the duration constant plus the two timeouts that scale with it.
- Verified: `npm test` (148/148, unchanged), `npx playwright test e2e/growth-transition.spec.ts` (4/4) and `e2e/full-playthrough.spec.ts` (1/1) both green against the new duration. Also did the actual look-at-it pass this app's conventions call for beyond automated checks: ran the real dev server, drove a real Stage 1 pass + Continue with Playwright, and captured it as a native Playwright video recording (webm) of the slowed transition — sent to the user directly rather than just described, since "does this look seamless" is a visual judgment call, not something a passing test proves. Did not re-run the full Playwright suite (43/43) this session — only the two specs that reference `GROWTH_DURATION`/the poll timeout were touched, and neither of the other 41 specs reads either constant.
- **Not yet done:** the Stage 2->3 transition and the Stage 3->Reveal non-animated boundary weren't separately re-captured on video (only Stage 1->2) — both are covered by the automated e2e re-run above, but if the user wants to *see* the 2->3 extrude specifically, that capture still needs to be produced.
- This is a tuning change to already-shipped Task 25 work, not a new numbered PLAN.md task — `Next up` above stays Task 27/28 (both optional, still untouched).
